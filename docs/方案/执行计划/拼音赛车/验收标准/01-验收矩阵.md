# 拼音赛车验收矩阵

| ID | 验收项 | 方法 | 通过标准 | 2026-07-29 结果 |
| --- | --- | --- | --- | --- |
| PINYIN-A01 | 拼音域隔离 | manifest/fixture | 只接受 `domain: pinyin` | 通过：专项和预集成 fixture |
| PINYIN-A02 | 直道真实运动 | 浏览器/Canvas 像素检查 | 车辆、目标、车道线随距离连续运动 | 通过：Canvas 非空且 cameraDistance 连续推进 |
| PINYIN-A03 | 固定背景不再主导 | 代码检查/截图 | 赛道由代码路段生成，可切换路线 | 通过：Canvas 代码赛道为主渲染器 |
| PINYIN-A04 | 题型分层 | 专项测试 | 声母、韵母、声调、汉字错误可区分 | 通过：题型和五类错误标签均有专项断言 |
| PINYIN-A05 | 学习时钟可读 | 专项测试/手工 | 播放/阅读时可暂停或减速，不遮挡题目 | 通过：路线态暂停；朗读期间 Canvas 为 `slowed/playback/0.5`，结束后恢复 `running/1` |
| PINYIN-A06 | 弯道和路段 | 浏览器 | 直道、弯道、S 弯至少各有一个可运行样例 | 通过：配置、透视渲染和路线弹层均有证据 |
| PINYIN-A07 | 刷新恢复 | snapshot test | 恢复赛道距离、当前卡和 session | 通过：专项快照恢复测试 |
| PINYIN-A08 | 共享文件不越权 | 文件清单 | P0/P1 未修改 SHOOTER 所有权文件 | 通过：本线程未修改共享入口 |

## 移动端弹层实测

- 视口：`360×800`；`innerWidth=360`、`innerHeight=800`、`scrollWidth=360`、`scrollHeight=800`。
- 答案态弹层：`x=25.45`、`y=363.74`、`width=309.11`、`bottom=547.54`；`an` 和 `ui` 两个按钮均在视口内。
- 分支态弹层：`x=25.45`、`y=357.74`、`width=309.11`、`bottom=553.54`；“内圈捷径”和“宽道恢复”文字均完整显示。
- 选择内圈后：路线面板隐藏，星门 DOM 可见，Canvas 仍在推进，页面日志未产生 error/warn。
- 朗读联动：点击“朗读当前任务”后 Canvas 为 `slowed/playback/0.5`；语音完成后恢复 `running/1`，新标签页 error/warn 日志为 `[]`。

## P2 宿主接线交接状态

截至 2026-07-29，P2 真实共享入口仍为 `review / fixture-only`。拼音专属预集成已通过 `domain/contentType`、原始 `cardId`、`returnContext`、snapshot/init、CORE v1 消息类型和无英语输入缓冲检查；新增 [`02-P2宿主接线交接.md`](../plan/02-P2宿主接线交接.md) 固化真实 `games/learning-arcade` 入口的接线顺序和 P2-A01..A08 验收条件。

以下项目必须由 SHOOTER 实施并由主控在共享入口浏览器中确认后，P2 才能改为 `verified`：

- 宿主真实发送 `init`，拼音 runtime 真实回传 `card-result`；
- `complete/stop` 和动画、语音、计时器 cleanup 真实生效；
- `returnContext` 在 init、card-result、complete、stop 之间保持可序列化一致；
- 刷新、重复 card-result、重复 complete 和 stop 后消息均按 CORE v1 幂等规则拒绝。
