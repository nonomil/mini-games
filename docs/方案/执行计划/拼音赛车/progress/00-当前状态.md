# 拼音赛车当前状态

```text
status: review
current_task: PINYIN-P2-01 / 共享宿主接线交接
owner_thread: PINYIN
last_verified_at: 2026-07-29
shared_runtime_gate: fixture-only; independent pinyin work is unblocked
shared_entry_owner: SHOOTER; main-control-confirmation-required
domain: pinyin
```

## 当前判断

- 最大问题是固定背景和 DOM 移动不像赛车，应优先建立代码化赛道。
- CORE-P0-05 与 SHOOTER-P1-01 已由主控复核为 verified；可继续独立模块和 P2 接入前的 fixture/计划核对。实际 `?game=word-cannon` 共享入口接线仍由 SHOOTER 负责并需主控确认。
- 已完成拼音域卡片契约、题型和错误标签，并建立不依赖共享入口的 track、Canvas renderer、runtime 和 snapshot 接口。
- P1-01 已补充稳定 16:9 viewport、移动端宽度约束、Canvas resize、直道目标卡、RAF 固定速度推进和 pause/resume；专项测试覆盖运行时调用和快照恢复。
- P1-02 已新增配置驱动的 learning-loop：直道、左右连续弯、S 弯、分叉、上下坡、桥、隧道和地标；branch choice 会改变有效路线长度并随 snapshot 恢复。
- P1-02 的 camera、车辆/目标投影、车道线、路面/桥/隧道/分叉/地标绘制和 collision 均使用 `distance/lateral/lane` 赛道坐标；未引入 DOM top 或固定背景运动。
- P1-03 已新增拼音专属 learning clock 和 metrics：reading/playback/hint/review/collision 可观察暂停原因，播放/提示支持减速，暂停和恢复不会推进或跳过赛道时间；状态进入 snapshot 并可恢复。
- P1-03 已将 collision、card-result、review-result 分开记录；错误结果保留 `wrong-initial/wrong-final/wrong-tone/wrong-character/missed-card`、原卡和原路线位置，错误卡进入去重的 review queue；复习成功先恢复位置再移除队列项。
- P1-03 的 runtime 统计独立记录 reaction/response window、speed、correctness、hint、review 和 collision，不写入 `profile`、`fsrs`、章节或 reward。
- 本轮完成独立赛车视觉接入：`raceCanvas` 作为主赛道，`pinyin-race-ui.js` 用 requestAnimationFrame 推进 learning-loop，Canvas 绘制透视路面、短车道虚线、红白路肩、路旁标记、地标路牌、拼音目标卡和带悬挂/尾焰的车辆；固定 WebP 没有承担主运动渲染。
- 本轮补充 `tests/test-pinyin-race-visuals.mjs`：先验证缺失 API 红灯，再验证 race render model 的连续道路片段、车身动态姿态、地标一次性入口、短虚线、目标投影和巡航状态同步。
- 本轮继续补齐赛车节奏：新增拼音专属 `pinyin-race-phase.js`，用纯函数定义 `3/2/1/GO!` 起步倒计时、暂停优先级和短暂冲线反馈；Canvas phase overlay 在倒计时冻结速度/赛道推进，冲线后保留 `finish` 反馈并递增 lap。
- 本轮继续补强赛车反馈层：Canvas 内增加速度/圈数 HUD、running/slowed/paused 状态带和接近终点才出现的方格拱门；`pinyin-race-ui.js` 记录并递增 lap，视觉速度继续由 learningClock multiplier 控制。
- 本轮浏览器验收（1440x900、390x844）：速度/圈数 HUD 清晰且不压住赛道；手动暂停显示“赛道暂停”并将速度归零；移动端无横向/纵向溢出；证据截图归档于拼音专属 `games/pinyin-star-scout/qa/pinyin-race-hud.png`、`pinyin-race-paused.png`、`pinyin-race-hud-mobile.png`。
- 浏览器验收（本地 `http://127.0.0.1:7003`）：桌面 1440x900 与移动 390x844 均 Canvas 非空、无横向/纵向溢出；Canvas cameraDistance 在约 420ms 内前进；左右变道按钮使 lane 从 1 切到 2；页面无运行时错误；证据截图保存在拼音专属 `games/pinyin-star-scout/qa/`。
- 本轮真实时序验收：页面初始与 500ms 时 `cameraDistance=0`，约 3 秒后开始推进；等待一整圈后读取到 `lap=2`、新圈 `cameraDistance=0` 和 `finishAt`；移动端 Canvas 为 `346×194.625`，桌面/移动均无溢出且 Canvas 像素非空。
- 本轮补充 `360×800` 极窄移动视口：答案态路线面板边界为 `x=25.45..334.55`、`y=363.74..547.54`，分支态为 `x=25.45..334.55`、`y=357.74..553.54`；答案按钮和“内圈捷径/宽道恢复”按钮文字完整，无横向溢出。
- 本轮移动端真实交互已完成：选择 `an` 进入路线分支，选择“内圈捷径”后路线面板隐藏，星门可见、赛道继续；页面开发日志读取结果为 `[]`。
- 本轮补充冻结卡片的拼音域校验，避免通过 `Object.freeze()` 绕过 `createPinyinQuestion()` 或 runtime 的域隔离；赛道投影累计前置弯道偏移，保证弯道连接直道时中心线连续。
- P2-01 preintegration 已建立拼音专属 fixture 和专项测试：复用 CORE v1 envelope 形状，验证拼音 `domain/contentType`、稳定 `sessionId/cardId`、payload 内 `returnContext`、`init` 内 snapshot 以及 runtime 不依赖英语输入缓冲。
- 当前 `word-cannon` 公共边界仍是 `petbank-learning-arcade` 的 legacy `source/kind/sessionId/seq/payload.activeGame`；本轮没有宣称真实 v1 `init/card-result`、returnContext 或共享入口已接通，P2 仍为 fixture-only，等待 SHOOTER/主控完成所有权交接。
- 当前专项测试验证纯函数、绘制调用、运行时状态、学习时钟和复习快照；独立 Canvas 像素、连续运动、移动布局、变道控件、路线态暂停和语音减速已有浏览器证据；仅 v1 宿主接线仍待后续。
- 本轮新增 `plan/02-P2宿主接线交接.md`，明确真实 `games/learning-arcade` 入口的消息顺序、拼音 domain/contentType 隔离、原始 cardId、returnContext、幂等、stop cleanup、任务分工和 P2-A01..A08 验收证据；fixture 通过不替代真实宿主验收。
- 本轮回归发现并修复 `pinyin-renderer.js` 的 `motionReduced` 作用域错误：render model 现在读取 `snapshot.reducedMotion`，普通 Canvas render 与 reduced-motion 冻结断言均恢复通过。
- 本轮补充 `pinyin-host-adapter.js`：严格模式拒绝非拼音、缺失原始 `cardId` 和泛化无 domain 卡；旧 `wordquest-host-init` 路径仅在显式兼容模式下使用稳定 fallback ID，并已由 `tests/test-pinyin-host-adapter.mjs` 验证。

## 当前边界

P1-03 已收束为 `verified`：专项、预集成、语法、全局测试和独立页面浏览器验收通过；路线态暂停和语音播放减速/恢复已有真实浏览器证据。P2-01 只完成 fixture/preintegration，当前没有独立开发阻塞，真实共享入口修改仍属于 SHOOTER 所有权并需主控确认。实际共享入口修改仍不得由本线程直接写入。当前剩余风险仅为真实 v1 宿主 init/card-result/returnContext 接线；移动端路线弹层和选择后继续态已补证。

CORE/SHOOTER 共享边界允许只读 fixture/计划核对；独立拼音模块当前没有 boundary gate，也不因共享入口交接暂停。PINYIN-P2-01 仍是 fixture-only，实际共享入口修改仍不得由本线程直接写入，必须由 SHOOTER 线程负责并经主控确认。
