# 拼音赛车执行日志

## 日志格式

```text
时间：
任务：
动作：
命令：
结果：
验收项：
风险/下一步：
```

## 日志

### 2026-07-27

时间：2026-07-27
任务：PINYIN-P0-01、PINYIN-P0-02（独立边界预备）
动作：读取主控计划、拼音赛车 plan/task/验收/progress 和拼音赛车优化方案；先写专项测试，再实现拼音域、数据、track、Canvas renderer、runtime 与 snapshot。
命令：`node tests/test-pinyin-racer.mjs`
结果：先以缺失模块红灯；补实现后 `PASS pinyin racer domain, track, renderer, and snapshot contracts`。
验收项：PINYIN-A01、PINYIN-A04 的数据/专项测试部分通过；PINYIN-A07 的 snapshot 数据恢复部分通过；PINYIN-A02/A03/A05/A06 的浏览器和完整 Canvas 验收未执行。
风险/下一步（主控复核前的历史记录）：当时 SHOOTER boundary 尚未验证，PINYIN-P2-01 共享入口接入暂缓；当前以最新日志为准，P2 前置 fixture/计划核对可继续，实际共享入口仍需 SHOOTER 负责。

补充验证：`npm test` 通过（小游戏契约、完整内容契约、完整模式运行时契约）；`git diff --check` 无错误。工作树中的 `games/learning-arcade/game.js` 已存在改动，本线程未修改该文件。

### 2026-07-27：PINYIN-P1-01

时间：2026-07-27
任务：PINYIN-P1-01
动作：先增加 viewport、直道目标投影、Canvas resize、RAF 推进、暂停/恢复和暂停快照恢复测试，再实现 renderer/runtime 的最小行为。
命令：`node tests/test-pinyin-racer.mjs`
结果：先因缺少 viewport API 红灯；实现后 `PASS pinyin racer domain, track, renderer, and snapshot contracts`。
验收项：P1-01 的稳定尺寸、直道运动、resize、移动端宽度约束、pause/resume、snapshot 和渲染调用测试通过。
风险/下一步（主控复核前的历史记录）：浏览器 Canvas 像素和真实窗口 resize 事件尚未手工验证；当时 SHOOTER boundary 未验证，P2 共享接入暂缓。当前共享边界已 verified，实际入口修改仍保留 SHOOTER 所有权。

补充验证：`node tests/test-pinyin-racer.mjs` 通过；`node --check` 通过（拼音 5 个模块及专项测试）；`git diff --check` 无错误。`npm test` 在既有地图契约阶段失败：缺少 `games/explore-map/forest-route.js`，超出 PINYIN 修改边界，未处理。

### 2026-07-27：P0-01/P0-02/P1-01 边界审计与回归

时间：2026-07-27
任务：PINYIN-P0-01；复核独立 runtime、快照和代码赛道的边界行为。
动作：为冻结的非拼音卡片域隔离和弯道连接后的中心线连续性补充专项失败测试；修正题目工厂/runtime 的卡片校验，并让 track projection 累计已通过路段的中心偏移；未修改共享入口。
命令：`node tests/test-pinyin-racer.mjs`（先红灯，修复后通过）；逐个执行 `node --check` 检查拼音 5 个模块和专项测试（通过）；`git diff --check`（通过）；`npm test`（通过）。
结果：`PASS pinyin racer domain, track, renderer, and snapshot contracts`；全局契约、地图、完整内容和完整模式测试均通过。
验收项：PINYIN-A01、PINYIN-A04、PINYIN-A07 的专项证据保持通过；PINYIN-A02/A03/A05/A06 仍未完成真实浏览器像素/手工验收；PINYIN-A08 通过文件范围审计，本线程未修改 `games/learning-arcade/game.js`、`host/bridge.js` 或英语词卡。
风险/下一步：共享边界已由主控复核为 verified，PINYIN-P2-01 前置 fixture/计划核对可继续；实际 `games/learning-arcade/game.js` 共享入口接入仍由 SHOOTER 负责并需主控确认。后续可在独立目录继续补复杂路段和学习时钟。

### 2026-07-27：主控复核后更新共享边界状态

时间：2026-07-27
任务：重新评估 PINYIN-P2-01 的共享边界阻塞。
动作：依据主控复核结果更新当前状态和任务清单；CORE-P0-05、SHOOTER-P1-01 均按主控通知视为 verified。未修改 `games/learning-arcade/game.js`、`bridge.js`、`host/` 或其他共享入口。
结果：PINYIN-P2-01 从 `planned` 调整为 `ready`；可继续独立拼音模块、fixture 和接入前计划核对。
验收项：共享边界 gate 已解除；保留 PINYIN-A08 越界约束，实际共享入口写入仍需 SHOOTER 线程负责并经主控确认。
风险/下一步：当前无 CORE/SHOOTER gate 阻塞；继续补独立复杂赛道、学习时钟或 P2 前置 fixture。未经 SHOOTER/主控确认，不直接修改共享入口。

### 2026-07-27：PINYIN-P2-01 preintegration fixture/计划核对

时间：2026-07-27
任务：PINYIN-P2-01 preintegration fixture/计划核对。
动作：新增 `games/pinyin-star-scout/pinyin-preintegration-fixture.js` 和 `tests/test-pinyin-preintegration.mjs`；只读复用 `tests/fixtures/contract-v1-fixture.mjs`、`host/manifests/pinyin-racer.game-manifest.json` 和 `games/learning-arcade/game.js` 的公开边界，没有修改 CORE、SHOOTER 或英语文件。
命令：`node tests/test-pinyin-preintegration.mjs`；`node tests/test-pinyin-racer.mjs`；`npm test`；拼音 fixture/测试 `node --check`；`git diff --check`。
结果：preintegration 专项通过；既有拼音专项通过；全局契约、地图、完整内容和完整模式测试通过。fixture 对齐 CORE v1 的 envelope 字段和六类消息，并把 `domain: pinyin`、`contentType: pinyin`、稳定 session/card、payload 内 returnContext 和 init snapshot 固化为预演数据。
验收项：已验证 fixture 层的 domain/contentType、session/card/returnContext、snapshot/init 和 runtime 无英语输入缓冲；已验证 word-cannon 当前 legacy `source/kind/sessionId/seq/payload.activeGame` 与 movement-only 键盘边界，legacy result 当前没有 cardId/returnContext；共享入口实际 v1 接入、宿主发送 init、card-result 回传和 returnContext 真实传递仍未验证。
风险/下一步：本轮新增内容全部是 fixture/静态边界证据，不是共享入口接入。PINYIN-P2-01 保持 `review`，后续交由 SHOOTER 线程和主控确认真实共享入口所有权与接线方案；本线程继续只写拼音专属目录和测试。

### 2026-07-27：PINYIN-P1-02

时间：2026-07-27
任务：PINYIN-P1-02
动作：先补配置路由、几何、camera metadata、分叉选择、统一坐标碰撞、复杂 renderer frame 和 branch snapshot 恢复测试；再实现 `pinyin-track-config.js`、track geometry/camera/collision、路面特征绘制和 runtime collision API。
命令：`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-preintegration.mjs`；拼音模块及两项专项测试 `node --check`；`npm test`；`git diff --check`；P1-02 范围审计 `rg -n "background|background-image|\.top|style\.top|target\.y|target\.top" games/pinyin-star-scout/pinyin-track.js games/pinyin-star-scout/pinyin-track-config.js games/pinyin-star-scout/pinyin-renderer.js games/pinyin-star-scout/pinyin-runtime.js`。
结果：专项输出 `PASS pinyin racer domain, track, renderer, and snapshot contracts`；preintegration 输出 `PASS pinyin preintegration fixture, word-cannon boundary, and no-English-buffer contracts`；语法检查、全局测试和 diff 检查通过；范围审计输出 `PASS no DOM top/background movement in P1-02 modules`。
验收项：已覆盖直道、curve-left/curve-right、S 弯、fork、slope-up/down、bridge、tunnel、landmark；分叉路线长度和 routeChoices 可恢复；camera 暴露 segment/path/height/landmark 状态；renderer 输出路段/地标并绘制车道、分叉、桥和隧道；collision 仅基于 distance/lateral/lane。
范围审计：本轮写入仅涉及 `games/pinyin-star-scout/`、`tests/test-pinyin-racer.mjs` 和本工作流 task/progress；未修改 `games/learning-arcade/game.js`、`host/bridge.js`、英语词卡、苦力怕、网球、地图或卡片系统。工作树中其他修改为既有并行工作流改动。
未完成项/下一步：真实浏览器 Canvas 像素、真实 window resize 事件和移动端布局仍未验收，故 P1-02 保持 `review`；P2 真实 v1 宿主接线仍由 SHOOTER/主控负责，现有 preintegration 仍为 fixture-only。

### 2026-07-27：PINYIN-P1-03 收束

时间：2026-07-27
任务：PINYIN-P1-03 学习时钟、碰撞和错误复习。
动作：复核现有 P1-01/P1-02 runtime、track、renderer、snapshot API；补充 reading/playback/hint/review 时钟、暂停原因、减速、collision、wrong answer、missed card、review queue、原路线位置、重复 card-result、review snapshot 和独立 metrics 测试；新增 `pinyin-learning.js`，扩展 runtime 的 clock、pause/slow/resume、collision、answer/review result、snapshot 恢复行为。
命令：`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-preintegration.mjs`；拼音模块和两项专项测试 `node --check`；`npm test`；`git diff --check`；P1-03 DOM/background 范围审计。
结果：首次专项红灯定位为 collision fixture 将起点车辆与 distance 110 的目标直接断言碰撞；修正 fixture 显式对齐车辆赛道位置后专项通过。第二次红灯复现复习成功先移除队列导致原卡位置无法恢复；修正为先恢复目标位置再移除队列项后，专项输出 `PASS pinyin racer domain, track, renderer, and snapshot contracts`。preintegration 输出 `PASS pinyin preintegration fixture, word-cannon boundary, and no-English-buffer contracts`；语法检查通过 11 个文件；`npm test` 和 `git diff --check` 通过；范围审计输出 `PASS P1-03 modules have no DOM-top/background movement references`。
验收项：已验证 reading/playback/hint/review/collision pause reason、slow multiplier、学习时钟和 snapshot 恢复；已验证 collision、wrong answer、missed card、review-result 分开记录，五类错误标签、原卡/路线位置、同一 cardId 的 card-result 去重，以及 reaction/response window/speed/correctness/hint/review/collision 统计互不替代。未将 fixture 或运行时统计写入 profile、FSRS、章节或 reward。
范围审计：本轮写入仅涉及 `games/pinyin-star-scout/pinyin-learning.js`、`games/pinyin-star-scout/pinyin-runtime.js`、`tests/test-pinyin-racer.mjs` 和本工作流 task/progress；未修改 `games/learning-arcade/game.js`、`host/bridge.js`、`host/`、英语词卡、苦力怕、网球、地图或卡片系统。工作树中其他修改为既有并行工作流改动。
未完成项/下一步：P1-03 保持 `review`，不得标为完整浏览器 verified；真实 Canvas 像素、window resize 事件、移动端布局和学习暂停/减速视觉证据尚未执行，Node/fixture 不能替代这些能力。P2 共享接入保持 `blocked`/fixture-only，真实 v1 宿主 init、card-result 回传、returnContext 传递和共享入口接线交由 SHOOTER/主控确认；本线程不开始 P2。

### 2026-07-29：PINYIN-P1 独立赛车视觉优化

时间：2026-07-29
任务：继续优化拼音赛车视觉，完成独立 Canvas 主赛道接入与浏览器验收。
动作：先新增 `tests/test-pinyin-race-visuals.mjs` 并确认 renderer 缺失 race model 的红灯；实现 `getPinyinRaceRenderModel()`、透视道路/短虚线/红白路肩/路旁标记/一次性地标/拼音目标路牌/车辆悬挂与尾焰；新增 `pinyin-race-ui.js` 用 `requestAnimationFrame` 驱动 learning-loop，通过现有拼音巡航状态同步目标卡，并提供键盘和移动端左右变道；更新独立页面 Canvas 主层与响应式布局。
命令：`node tests/test-pinyin-race-visuals.mjs`；`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-preintegration.mjs`；`npm test`；拼音模块 `node --check`；`git diff --check`；本地服务 `http://127.0.0.1:7003` 的 Chromium 桌面/移动视口检查。
结果：三项拼音专项和 `npm test` 通过；桌面 1440x900、移动 390x844 Canvas 均有非空像素且无页面溢出；cameraDistance 在约 420ms 内前进；左右变道按钮将 lane 从 1 切到 2；浏览器页面无 runtime error；证据截图归档于拼音专属 `games/pinyin-star-scout/qa/`。
验收项：独立 Canvas 代码赛道主渲染、车道/车辆/目标投影、移动布局和变道控件已有证据；固定 WebP 未作为主赛道渲染器；新增测试覆盖 race frame、连续道路片段、短虚线、一次性地标和巡航状态同步。
边界/风险：本轮只写 `games/pinyin-star-scout/`、拼音专属测试和自身 progress；没有修改 `games/learning-arcade/game.js`、`host/bridge.js`、host 或英语词卡。P2-01 仍是 fixture-only，真实 v1 宿主 init/card-result/returnContext 和共享入口接线仍由 SHOOTER/主控按所有权交接；独立拼音工作不再因过期 boundary gate 阻塞。真实学习暂停/减速与 Canvas 的视觉联动仍待补证。

### 2026-07-29：PINYIN-P1 移动端路线弹层验收

时间：2026-07-29
任务：补齐 `PINYIN-A02`、`PINYIN-A03`、`PINYIN-A06` 的移动端路线交互证据，并复核 `PINYIN-A05` 的剩余缺口。
动作：在本地页面设置 `360×800` 视口；从干净局面收集 `sh`，进入答案态路线弹层；选择 `an` 进入分支态；选择“内圈捷径”后复核路线面板隐藏、星门可见和赛道继续。
命令：浏览器本地验收；`tab.dev.logs({ levels: ['error', 'warn'] })`。
结果：答案态面板为 `x=25.45..334.55`、`y=363.74..547.54`；分支态面板为 `x=25.45..334.55`、`y=357.74..553.54`；两个状态的内容宽度均为 360，无横向/纵向滚动溢出；答案按钮和路线按钮文字完整；选择后 `routePanel.hidden=true`、`board-portal` 可见；页面日志为 `[]`。
验收项：`PINYIN-A02`、`PINYIN-A03`、`PINYIN-A06` 的移动端证据补齐；`PINYIN-A05` 的 runtime 证据保持通过，但真实学习暂停/减速视觉联动仍未宣称完成。
边界/风险：本轮只写拼音方案文档和读取/操作拼音独立页面，没有修改共享入口；浏览器临时视口验收结束后已恢复默认视口并清理临时标签页。P2-01 仍为 fixture-only。

### 2026-07-29：PINYIN-P1 文档收束与全量验证

时间：2026-07-29
任务：更新方案/任务/验收状态，并执行本轮最终验证。
动作：补充实施计划的阶段退出条件、任务清单状态、验收矩阵移动端实测记录、优化方案落地状态和当前进度；随后运行拼音专项、全局测试、语法检查和差异检查。
命令：`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-scout-session.mjs`；`node tests/test-pinyin-preintegration.mjs`；`node tests/test-pinyin-race-visuals.mjs`；`npm test`；拼音模块 `node --check`；`git diff --check`。
结果：四个拼音专项均输出 `PASS`；`npm test` 通过 11 个小游戏契约、数学冒险、数学 PK、3 张地图、森林纵向切片、探索会话、6324 张完整内容卡和完整模式运行时契约；语法检查退出码 0；`git diff --check` 无空白错误，仅有既有文件的 LF/CRLF 转换提示。
验收项：P0-02、P1-01、P1-02 的任务状态更新为 `verified`；P1-03 保持 `review`，因为真实学习暂停/减速与 Canvas 的视觉联动仍未完成；P2-01 保持 `review / fixture-only`。
边界/风险：本轮没有修改 `games/learning-arcade/game.js`、`bridge.js`、`host/` 或英语词卡；所有共享入口接线仍由 SHOOTER/主控负责。

### 2026-07-29：PINYIN-P1-03 暂停/减速真实联动收束

时间：2026-07-29
任务：完成学习时钟到 Canvas 的真实桥接，并验证语音播放生命周期。
动作：先为 race snapshot、暂停步进、减速步进和 session bridge 编写失败断言；实现 `getPinyinRaceLearningState()`、`getPinyinRaceStepDistance()`、路线态暂停、`slow()` session bridge 和语音播放 slow/resume；更新入口资源版本号清除旧 ESM 缓存。
命令：`node tests/test-pinyin-race-visuals.mjs`；`node tests/test-pinyin-scout-session.mjs`；浏览器点击“朗读当前任务”并读取 Canvas `data-learning-mode/data-learning-reason/data-speed-multiplier`。
结果：路线态由专项断言得到 `paused/review/0`；播放态浏览器实测 `slowed/playback/0.5`；等待语音完成后实测恢复 `running/1`；新标签页 error/warn 日志为 `[]`。
验收项：`PINYIN-A05` 完成；`PINYIN-P1-03` 更新为 `verified`。P2-01 仍为 `review / fixture-only`。
边界/风险：本轮只修改拼音独立模块、专项测试、入口资源版本号和拼音方案文档；没有修改共享入口。浏览器临时标签和 `360×800` 覆盖在验收结束后清理/恢复。

### 2026-07-29：PINYIN-P1-03 最终回归

时间：2026-07-29
任务：确认暂停/减速桥接不会破坏既有拼音和全局契约。
命令：`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-scout-session.mjs`；`node tests/test-pinyin-preintegration.mjs`；`node tests/test-pinyin-race-visuals.mjs`；`npm test`；拼音模块 `node --check`；`git diff --check`。
结果：四个拼音专项均输出 `PASS`；全局测试通过 11 个小游戏契约、数学冒险、数学 PK、3 张地图、森林纵向切片、探索会话、6324 张完整内容卡和完整模式运行时契约；语法检查退出码 0；差异检查无空白错误，仅保留既有 LF/CRLF 转换提示。
验收项：`PINYIN-P1-03` 保持 `verified`；`PINYIN-P2-01` 保持 `review / fixture-only`。
边界/风险：未修改 `games/learning-arcade/game.js`、`bridge.js`、`host/` 或英语词卡；真实 v1 宿主接线仍等待 SHOOTER/主控所有权交接。

### 2026-07-29：PINYIN-P1-03 赛车反馈层继续优化

时间：2026-07-29
任务：继续提升赛车临场感，补齐速度/圈数/暂停/冲线反馈。
动作：先在 `tests/test-pinyin-race-visuals.mjs` 增加 running/slowed/paused telemetry、lap 快照和终点接近条件的失败断言；实现 Canvas 速度/圈数 HUD、减速/暂停状态带、终点方格拱门，并让 `pinyin-race-ui.js` 在赛道循环时递增 lap。
命令：`node tests/test-pinyin-race-visuals.mjs`；Chromium 本地页面 1440x900、390x844 验收；手动调用 `PinyinRaceCanvas.pause()/resume()` 验证暂停反馈。
结果：专项测试通过；桌面 HUD 显示速度 148 KM/H 与第 1 圈；暂停态显示“赛道暂停”、速度为 0；移动视口 Canvas 为 `346×194.625`，页面尺寸与视口一致，无溢出；截图归档于 `games/pinyin-star-scout/qa/`。
验收项：running/slowed/paused 三态有明确视觉反馈；速度显示受 learningClock multiplier 影响；终点拱门条件由 track distance 决定；不使用固定背景或共享入口。
边界/风险：只写拼音 renderer、race UI、专项测试和自身 task/progress；P2-01 仍为 fixture-only，真实 v1 宿主接线继续等待 SHOOTER/主控所有权交接。

### 2026-07-29：PINYIN-P1-03 起步与冲线节奏优化

时间：2026-07-29
任务：继续提升独立拼音赛车的起步、加速和冲线临场感。
动作：遵循先红后绿，在 `test-pinyin-race-visuals.mjs` 增加起步 `3/2/1/GO!`、暂停优先级、冲线反馈和 Canvas phase overlay 断言；新增 `pinyin-race-phase.js` 纯函数；renderer 接入 countdown/finish telemetry 与 overlay；race UI 接入可暂停的 race clock、倒计时冻结、冲线时间戳和 lap reset feedback。
命令：`node tests/test-pinyin-race-visuals.mjs`；`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-scout-session.mjs`；`node tests/test-pinyin-preintegration.mjs`；拼音模块 `node --check`；`git diff --check`；本地 Chromium 时序/响应式验收。
结果：专项输出 `PASS pinyin race visual contracts`；其余三个拼音专项均输出 `PASS`；语法检查退出码 0；真实页面初始/500ms 的 `cameraDistance` 均为 `0`，约 3 秒后开始推进；完整一圈后读取 `lap=2`、新圈距离 `0` 和 `finishAt`；桌面/移动 Canvas 像素非空且无页面溢出。
验收项：起步倒计时不提前消耗赛道距离；GO 后车速/路面推进恢复；复习/手动暂停优先显示暂停态；冲线反馈独立于 CORE/session，不改变拼音卡片或共享入口协议。
边界/风险：本轮仅写拼音专属 phase/renderer/race UI、拼音专项测试和自身 progress；未修改 `games/learning-arcade/game.js`、`host/bridge.js`、host 或英语词卡。独立拼音工作无 boundary gate；P2-01 仍为 fixture-only，真实共享入口接线仍由 SHOOTER 负责并需主控确认。

### 2026-07-29：PINYIN-P2-01 接线交接与回归修复

时间：2026-07-29
任务：补齐 P2 共享宿主接线交接证据，并修复回归测试暴露的拼音渲染器错误。
动作：审查现有 manifest、CORE v1 bridge、生命周期契约和 `learning-arcade` 所有权；新增 `plan/02-P2宿主接线交接.md`，固化 `ready/init/card-result/complete/stop` 顺序、拼音 domain 隔离、returnContext、幂等、stop cleanup、任务清单和 P2-A01..A08 验收；回归时发现 `getPinyinRaceRenderModel()` 使用了未定义的 `motionReduced`，补充从 `snapshot.reducedMotion` 读取该状态。
命令：`node tests/test-pinyin-racer.mjs`；`node tests/test-pinyin-scout-session.mjs`；`node tests/test-pinyin-preintegration.mjs`；`node tests/test-pinyin-race-visuals.mjs`；`node tests/contract-cross-game-protocol.mjs`；`node tests/contract-lifecycle.mjs`；拼音目录 `node --check`；`npm test`；`git diff --check`。
结果：以上命令均通过；四个拼音专项、两个 CORE v1 共享契约和全局测试通过；全局测试覆盖 11 个小游戏契约、3 张地图、森林纵向切片、探索会话、6324 张完整内容卡和完整模式运行时契约。`git diff --check` 无空白错误，仅有既有 LF/CRLF 转换提示。
验收项：P1-03 保持 `verified`；P2-01 仍为 `review / fixture-only`。交接文档已可直接供 SHOOTER/主控实施，但真实 `games/learning-arcade` 宿主 init、card-result、complete、stop 和 returnContext 仍未在本线程宣称完成。
边界/风险：本轮只新增拼音方案交接文档并修改拼音专属 `pinyin-renderer.js`；没有修改 `games/learning-arcade/game.js`、`bridge.js`、`host/` 或英语词卡。共享入口接线仍由 SHOOTER 实施并由主控验收。

### 2026-07-29：PINYIN-P2 独立卡片适配器补强

时间：2026-07-29
任务：修复独立页旧宿主初始化路径对缺失 `applyHostHanzi` 的调用，并为拼音卡接收增加严格 domain 隔离。
动作：先新增 `tests/test-pinyin-host-adapter.mjs` 红测，覆盖标准 CORE 卡、英语卡拒绝、缺少原始 cardId 拒绝、旧格式稳定 fallback ID 和批量过滤；实现 `pinyin-host-adapter.js`，在 `game.js` 的 `wordquest-host-init` 路径接入 `normalizePinyinHostCards`。严格模式不把无 domain 的泛化卡当拼音；旧入口仅显式 `assumePinyin` 后兼容。
命令：`node tests/test-pinyin-host-adapter.mjs`；拼音目录 `node --check`。
结果：适配器专项输出 `PASS pinyin host card adapter contracts`；拼音目录语法检查通过。该适配器只补独立页的防御和卡片标准化，不构成 `games/learning-arcade` 的真实 CORE v1 接线。
验收项：拼音卡保留原始 `cardId`、声调显示/比较字段和 `contentType=pinyin`；英语卡不会进入独立拼音 session；无宿主 cardId 的 CORE v1 输入不会被伪造 ID 接受。
边界/风险：本轮仍未修改共享入口；P2-01 保持 `review / fixture-only`，真实 `ready/init/card-result/complete/stop` 和 returnContext 回传仍由 SHOOTER/主控负责。
