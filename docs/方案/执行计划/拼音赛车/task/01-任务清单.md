# 拼音赛车任务清单

| ID | 任务 | 文件范围 | 依赖 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| PINYIN-P0-01 | 定义拼音卡片、题型和错误标签 | 拼音数据/测试 | 无 | verified | `node tests/test-pinyin-racer.mjs` 通过；卡片字段、原始显示值/标准化 key/声调、六类题型和五类错误标签由 `pinyin-domain.js` 定义；冻结的非拼音卡片也会被拒绝 |
| PINYIN-P0-02 | 建立专属 runtime 和快照接口 | 新建拼音专属目录 | P0-01 | verified | `node tests/test-pinyin-racer.mjs` 覆盖 track、renderer、runtime、snapshot；runtime 校验拼音域卡片；独立 Canvas 页面已在桌面和移动视口加载，未修改共享入口 |
| PINYIN-P1-01 | Canvas 直道 MVP | 拼音专属目录 | P0-02 | verified | `node tests/test-pinyin-racer.mjs` 覆盖 viewport、直道投影、连续弯道中心线、RAF 推进、暂停/恢复和 snapshot；`tests/test-pinyin-race-visuals.mjs` 与 Chromium 桌面、`390×844`、`360×800` 验收确认 Canvas 非空、连续运动、响应式布局和变道控件 |
| PINYIN-P1-02 | 路段数据、弯道和分叉 | 拼音专属目录、配置 | P1-01 | verified | `node tests/test-pinyin-racer.mjs` 覆盖配置路段、连续弯道/S 弯、分叉路线、camera metadata、桥/隧道/地标、统一坐标碰撞和 branch snapshot；浏览器确认代码化透视路面、短虚线、路肩、地标路牌、车辆主渲染，以及真实答案态/分支态路线弹层 |
| PINYIN-P1-03 | 学习时钟、碰撞和错误复习 | 拼音专属目录、测试 | P1-02 | verified | 先以红测复现碰撞 fixture、复习位置恢复和 race phase 缺失问题，再通过 `node tests/test-pinyin-racer.mjs`、`node tests/test-pinyin-scout-session.mjs`、`node tests/test-pinyin-race-visuals.mjs`；覆盖阅读/播放/提示/复习暂停或减速、pause reason、clock/snapshot、碰撞与错误分离、五类错误标签、原路线位置恢复、重复 card-result 拒绝、reaction/response window/speed/correctness/hint/review 独立统计；浏览器确认路线态 Canvas 暂停、朗读期间 `slowed/playback/0.5`，起步 `3/2/1/GO!` 冻结距离、冲线后 lap 递增和 finish feedback，结束后恢复 `running/1`，速度/圈数 HUD、暂停状态带、终点方格门条件通过，页面无运行时错误 |
| PINYIN-P2-01 | 与 learning-arcade/宿主接入 | 共享入口仅限 SHOOTER 合并 | SHOOTER boundary verified、CORE verified | review / fixture-only | `node tests/test-pinyin-host-adapter.mjs`、`node tests/test-pinyin-preintegration.mjs`、`node tests/contract-cross-game-protocol.mjs`、`node tests/contract-lifecycle.mjs` 通过但仍为拼音专属 fixture；新增 [`02-P2宿主接线交接.md`](../plan/02-P2宿主接线交接.md)，固化真实 `ready/init/card-result/complete/stop`、domain 隔离、returnContext、幂等、cleanup 和浏览器验收；独立页已补严格/兼容卡片适配器和旧 init 入口防御，但静态确认 word-cannon 仍是 legacy `source/kind/sessionId/seq/payload.activeGame` 边界，真实 v1 入口接入、宿主 init、card-result 回传和 returnContext 传递未完成；当前限制来自共享入口所有权交接，不阻塞独立拼音模块；本线程不修改共享入口 |

## 越界约束

- P0/P1 不修改 `games/learning-arcade/game.js`。
- 不使用英语 `word-shooter` 的单词答案或复习字段。
- 不把固定 WebP 背景当作赛道主渲染器。
- 不用车速直接改变拼音掌握度。
