# 网球学英语当前状态

```text
status: review
current_task: TENNIS-P1-02-验收分层
owner_thread: TENNIS
last_verified_at: 2026-07-27
content_type: english-expression
```

## 当前判断

- 网球游戏承担表达/语境辨析，不承担单词拼写和语法掌握度。
- 已创建独立 `games/tennis-english/` 入口，默认 7 球短局可在独立页面运行。
- P1-01/P1-02 已验证：8 张表达卡、三选一候选、发球/选择/回球/反馈/下一球/完成状态、错误意义/搭配/语境说明均已实现。
- P1-03 已验证：完成页列出错表达、错误类型和中英文解释；全对时显示空错题状态，部分错误时可进入 6 球错题复习局或创建全新标准再玩局。
- P1-04 已验证：网球 manifest 声明 CORE v1 能力；独立协议 fixture 覆盖 ready/init/card-result/complete/stop/error、身份字段、returnContext、卡片 domain/contentType、幂等和 stop cleanup。
- 运行时只保留局内比分和学习证据，不读取或写入卡片系统、宿主桥接或现有游戏。

## 证据分层

| 证据层 | 当前状态 | 证据边界 |
| --- | --- | --- |
| P1-01/P1-02 契约与逻辑 | complete | `node tests/test-tennis-english.mjs`、`npm test`、JS 语法检查和 manifest JSON 校验通过；覆盖表达卡、状态机、6-8 球、三选一、错误标签和输入代码路径。 |
| HTTP 独立入口可达 | reachable | 本地静态服务 `http://127.0.0.1:7004/games/tennis-english/` 与 `game.js` 返回 HTTP 200；这只证明资源可达。 |
| 真实浏览器交互/截图 | complete | Playwright 本地服务 `http://127.0.0.1:4173/games/tennis-english/` 下完成桌面入口/发球、pointer 错误反馈、键盘 `1` 正确回球、360x800 触摸 pointerdown、7 球完成页和复习入口；保存 ready、移动反馈和移动完成截图。 |
| CORE/卡片系统跨项目接线 | pending | 目前只有独立协议 fixture；未接入真实 `postMessage`、`host/bridge.js` 或卡片系统，不能宣称宿主闭环。 |

线程总体保持 `review`，不标记为 full verified。

## 阻塞

浏览器验收已完成，无浏览器能力阻塞。Playwright 控制台唯一错误为独立入口缺少 `/favicon.ico` 的静态 404，不影响页面运行；需补 favicon 时另行处理。真实跨项目协议接线仍未完成，需宿主适配工作流接入后复测生命周期，因此线程总体继续保持 `review`。
