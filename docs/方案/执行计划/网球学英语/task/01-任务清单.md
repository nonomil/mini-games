# 网球学英语任务清单

| ID | 任务 | 文件范围 | 依赖 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| TENNIS-P1-01 | 表达卡和回合状态机 | `games/tennis-english/` 新目录 | 无 | complete | `node tests/test-tennis-english.mjs`：表达卡字段、混淆集、状态迁移、比分和错误标签通过 |
| TENNIS-P1-02 | 完成独立选择回球 MVP | 新目录 HTML/JS/CSS | P1-01 | complete | `node tests/test-tennis-english.mjs`：默认 7 球、三选一、pointer/touch、键盘选择与回球通过；真实 Playwright 复核桌面 pointer、键盘 `1`、360x800 touch pointerdown；P1-02 验证时 `npm test` 无回归 |
| TENNIS-P1-03 | 错误反馈、结果页和复习出口 | 新目录、测试 | P1-02 | complete | `node tests/test-tennis-english.mjs`：全对空集合、部分错误解释、6 球复习局、再玩新 session 和完成页入口通过；真实浏览器完成 7 球后显示错误解释并进入“复习错题”6 球局 |
| TENNIS-P1-04 | manifest 和宿主生命周期 | 新目录、CORE fixture | CORE-P0-03/P0-04 | complete | `node tests/test-tennis-english.mjs`：manifest 能力声明、ready/init/card-result/complete/stop/error envelope、身份字段、returnContext、幂等和 stop cleanup 通过；真实跨项目接线未完成 |
| TENNIS-P1-05 | 语法塔扩展接口说明 | `games/tennis-english/grammar-tower-extension.js`、专项测试 | P1-04 | complete | `node tests/test-tennis-english.mjs`：主题/楼层/章节 `grammar-tower` 上下文、`returnContext` 传递和 positive floor 校验通过；AI 仅允许 `explanation`/`example-candidate`/`npc-copy` 候选，强制人工复核并拒绝 answer、targetCardId、distractor、mastery 和 review scheduling 字段；不生成或修改答案、干扰项或长期掌握度 |

## 证据分层

| 类别 | 状态 | 当前证据 | 未证明内容 |
| --- | --- | --- | --- |
| P1-01/P1-02 契约完成 | complete | 网球专项测试、`npm test`、JS 语法检查和 manifest JSON 校验通过；表达卡、回合状态机、6-8 球、三选一、错误说明和 pointer/touch/keyboard 代码路径均有覆盖。 | 不替代真实浏览器操作验收。 |
| HTTP 可达 | reachable | `http://127.0.0.1:7004/games/tennis-english/` 和 `game.js` 返回 HTTP 200。 | 只证明静态资源可访问，不证明页面交互。 |
| 浏览器交互/截图 | complete | Playwright 本地服务下完成入口、发球、桌面 pointer 错误反馈、键盘 `1`、360x800 touch pointerdown、完成页和复习出口；有桌面 ready、移动反馈、移动完成截图。 | Playwright 控制台有独立入口 `/favicon.ico` 404；不影响网球页面交互。 |
| CORE/卡片系统跨项目接线 | pending | 仅有独立协议 fixture，未修改 `host/bridge.js` 或卡片系统。 | 真实 `postMessage` 生命周期和跨项目结果回传。 |

| P1-05 语法塔边界 | complete | `grammar-tower-extension.js` 与专项契约测试覆盖上下文字段、正整数楼层、人工复核策略和 AI 禁止字段；协议 fixture 可携带该 `returnContext`。 | 仅完成独立适配层；语法塔真实入口和跨项目宿主接线未实现。 |

线程总体状态：`review`，不等同于 full verified。
