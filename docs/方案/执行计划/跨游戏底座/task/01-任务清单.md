# 跨游戏底座任务清单

| ID | 任务 | 文件范围 | 依赖 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| CORE-P0-01 | 盘点已有 bridge、shim 和消息类型 | `host/`、`bridge.js`、`tests/` | 无 | verified | `host/mini-game-host-protocol.md` 现状兼容字段表；专项契约覆盖 legacy bridge、shim 和三个旧式 source；`npm test` 全部通过；2026-07-27 09:34 本轮复核 cross-game/lifecycle/完整契约/语法检查均通过 |
| CORE-P0-02 | 建立 manifest 和卡片标准化 | `host/`、各清单文件 | P0-01 | verified | `host/game-manifest.schema.json`、三个 `host/manifests/*.game-manifest.json`；标准字段 `cardId/word/translation/image/audio/example/domain/contentType`；专项契约静态读取通过；2026-07-27 09:34 本轮复核 3 个 manifest 与 schema JSON 解析通过 |
| CORE-P0-03 | 实现生命周期和结果幂等 | `host/`、`bridge.js` | P0-02 | verified | `bridge.js` 提供 `ready/error/reportCardResult/complete/stop/registerCleanup`；`host/petbank-host-shim.js` 提供 cleanup；`tests/contract-lifecycle.mjs` 覆盖顺序、幂等、stop 后拒绝、origin/session 和旧 bridge 兼容；`node --test`、专项、语法和 `npm test` 通过 |
| CORE-P0-04 | 增加安全、停止和恢复测试 | `tests/` | P0-03 | verified | `tests/contract-lifecycle.mjs` 覆盖刷新恢复同 session/game/card、未知/过期 card、origin/source/session/game/card 值与类型矩阵、error code/message、stop listener/timer cleanup、timeout 和重复 complete；`node --test`、cross-protocol、`npm run test:contract`、`npm test`、语法检查和 `git diff --check` 通过 |
| CORE-P0-05 | 发布 fixture 和交接说明 | `host/`、`tests/fixtures/`、`tests/`、本目录 `progress/` | P0-04 | verified | `tests/fixtures/contract-v1-fixture.mjs` 发布 ready/init/card-result/complete/stop/error、恢复状态、timeout 和 legacy 示例；`host/mini-game-host-protocol.md` 第 8 节提供可复制接线说明；lifecycle 契约逐字段验证实际 bridge API；专项、完整测试、语法和范围审计通过 |

## 每项任务的交付要求

- P0-01：提交现状消息表和不兼容点，不修改游戏逻辑。
- P0-02：清单可以被静态读取，至少声明 `gameId/domains/capabilities`。
- P0-03：一张卡最多一个 `card-result`，完成事件只发送一次。
- P0-04：命令输出和失败场景写入日志。
- P0-05：给其他线程一段可复制的初始化 payload 和结果 payload。
