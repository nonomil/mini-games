# 跨游戏底座当前状态

```text
status: verified
current_task: ADAPTER/TENNIS 接线前置
owner_thread: CORE
last_verified_at: 2026-07-27 09:34:38 +08:00
protocol_version: draft-v1
```

## 已知事实

- 当前项目已有 `host/petbank-host-shim.js`、`bridge.js` 和部分 bridge 消息。
- 需要保留已有 `petbank.bridge.v1` 兼容能力，再抽象通用学习游戏协议。
- 其他线程只能依赖本文件中标记为 `verified` 的字段。
- CORE-P0-01 已完成：现状入口、shim、bridge、旧式消息和契约测试已盘点，兼容字段表写入 `host/mini-game-host-protocol.md`。
- CORE-P0-02 已完成：三个逻辑游戏 manifest 和 v1 schema 已写入 `host/`，卡片字段规则明确为原始 `cardId` 加七个标准字段。
- `word-shooter` 与 `pinyin-racer` 共用 `learning-arcade` 入口，分别通过 `runtime.mode` 标识 `word-shooter`/`word-cannon`。
- CORE-P0-03 已完成：`bridge.js` 提供 v1 `ready/init/card-result/complete/stop/error` 处理和公开 API；同一 `sessionId + cardId` 的结果只发送一次，complete 只发送一次，stop 后拒绝结果/完成/旧 completion。
- `host/petbank-host-shim.js` 提供一次性 `MiniGamesHost.registerCleanup/cleanup`，bridge stop 会执行 bridge 与 shim cleanup。
- 旧 `petbank.bridge.v1.completed/reward-result` 和旧 `source/kind/payload` 兼容路径保留，已通过回归测试；不需要拆成下一任务。
- CORE-P0-04 已完成：刷新恢复同一 session/game/card 和结果幂等状态；未知/过期 card 拒绝；origin/source/session/game/card 身份矩阵拒绝；error code/message、timeout、重复 complete 和 stop 后终态均已覆盖。
- v1 入站身份字段按协议类型校验，非字符串 `cardId` 不会被强制转换后接受。
- `error({ code, message, payload })` 会保留额外 payload，并补齐/优先使用显式 `code/message`；`armTimeout` 只允许一个活动计时器，到期进入 timeout 终态。
- stop 立即清理 bridge/shim cleanup、活动 timeout 和 message listener；刷新恢复 stop 终态时不重新挂载 listener。
- CORE-P0-05 已完成：`tests/fixtures/contract-v1-fixture.mjs` 是 v1 fixture 单一来源，覆盖六类消息、字符串身份、标准卡片字段、恢复状态、幂等、timeout、stop cleanup 和 legacy；协议文档第 8 节可直接交给 ADAPTER/TENNIS/MAP/SHOOTER/CREEPER 接线。

## 已验证证据

- `node tests/contract-cross-game-protocol.mjs`：`PASS cross-game protocol contract: 3 manifests`。
- `npm test`：现有三组契约通过，分别为 `mini games contract passed: 11 games`、`PASS complete content contract: 6324 cards`、`PASS complete mode runtime contract`。
- 最终 `npm test` 已包含并通过并发线程加入的 `test:map`：`PASS map contract: 3 maps`、`PASS exploration session contract`，同时保留上述完整内容和运行时契约通过。
- `node --check bridge.js`、`node --check host/petbank-host-shim.js`：通过。
- `node --test tests/contract-lifecycle.mjs`：TAP 1 test、1 pass、0 fail；覆盖 P0-04 恢复、安全矩阵、卡片拒绝、timeout 和清理。
- `npm run test:contract`：`mini games contract passed: 11 games`。
- `git diff --check` 和 CORE 目录尾随空白审计：通过；仅有工作区既有 LF/CRLF 转换提示。
- `tests/contract-lifecycle.mjs` 导入 fixture 并逐结构比较实际 bridge 的 ready/card-result/error/complete/stop/timeout/legacy 消息；恢复 storage、重复调用和 cleanup 断言通过。
- `node --check tests/fixtures/contract-v1-fixture.mjs`：通过。
- 2026-07-27 09:34 本轮复核：cross-game protocol、lifecycle、`npm run test:contract`、`npm test`、bridge/shim/fixture 语法检查及 4 个 manifest/schema JSON 解析全部通过。

## 文件范围审计

- CORE-P0-05 本轮写入仅限 `host/mini-game-host-protocol.md`、`tests/fixtures/contract-v1-fixture.mjs`、`tests/contract-lifecycle.mjs` 和本工作流 `task/progress/` 范围内；此前 CORE 的 bridge/manifest 文件仍在允许范围内。
- `games/` 业务玩法文件不是本轮写入目标；最终审计发现其中存在其他线程的修改和未跟踪文件，CORE 未触碰或回滚。

## 阻塞

暂无。CORE-P0-03、CORE-P0-04、CORE-P0-05 均已 verified；ADAPTER/TENNIS 可依据 fixture 进行真实接线，CORE 暂无新的协议实现任务。
