# 全局验收矩阵

## A. 独立运行

| 编号 | 验收项 | 证据 | 状态 |
| --- | --- | --- | --- |
| A-01 | 不连接宿主时四个游戏仍可启动 | 独立模块/入口契约与 HTTP 证据；完整浏览器走查未覆盖全部游戏 | partial |
| A-02 | 现有游戏入口、完整词库和完整内容模式不退化 | `npm test`：11 games、6324 cards、complete mode 通过 | verified |
| A-03 | 每局有开始、暂停、完成、再玩、复习错题、返回入口 | TENNIS 真实浏览器已验证开始、7 球完成、复习入口和 6 球复习局；SHOOTER 已真实看到暂停/恢复、6 目标页面、结果复习/再玩/返回入口；MAP 已真实完成 50/50 并返回原节点；CREEPER 完整闭环、统一跨游戏流程和其余证据仍未完成 | partial |
| A-04 | 移动端 360x800 不发生主要遮挡 | TENNIS 真实 `360x800` 已验证选项纵排、反馈/结果分离、`bodyScrollWidth=360` 且无视觉重叠；SHOOTER 390x844 方向控件无横溢出；MAP 真实 `360x800` 已验证 `document/body scrollWidth=clientWidth=345`、故事壳收缩为 `345px` 且无横溢出；其余游戏截图未完成 | partial |

## B. 学习证据

| 编号 | 验收项 | 证据 | 状态 |
| --- | --- | --- | --- |
| B-01 | 独立答对、提示后答对、错误、跳过分别记录 | CREEPER learning-loop tests；adapter fixture 覆盖 card-result | partial |
| B-02 | 每张卡最多提交一次结果 | CORE lifecycle + adapter fixture 重复结果测试 | verified |
| B-03 | 错题进入复习队列，当前局不直接清空长期资源 | CREEPER 本地 nextReviewAt、TENNIS review set、MAP 上下文测试 | partial |
| B-04 | 游戏分数和 XP 不直接修改掌握度 | CREEPER/ADAPTER 代码和 fixture；真实宿主结算未接线 | partial |
| B-05 | 拼音、英语、英语表达使用不同 domain/contentType | manifest、拼音专项和网球表达卡契约 | verified |

## C. 宿主和恢复

| 编号 | 验收项 | 证据 | 状态 |
| --- | --- | --- | --- |
| C-01 | `ready -> init -> card-result -> complete` 生命周期可完成 | `node --test tests/contract-lifecycle.mjs`：1 pass | verified |
| C-02 | `stop()` 停止计时器、动画、声音和监听器 | CORE lifecycle cleanup + adapter 21/21 fixture | partial |
| C-03 | 刷新恢复原 `sessionId`、卡片和上下文 | CORE refresh recovery、MAP exploration session、adapter returnContext fixture | verified |
| C-04 | origin、sessionId、gameId、cardId 均校验 | CORE P0-04 类型/值/来源矩阵测试 | verified |
| C-05 | 游戏结束返回原章节或地图节点 | MAP 真实浏览器已完成 50/50 并从完成页返回原地图节点，returnContext 保留 map/route/node/task/session/剩余上下文；adapter 真实跨项目尚未接入，其他游戏仍需接线验证 | partial |

## D. 地图和章节

| 编号 | 验收项 | 证据 | 状态 |
| --- | --- | --- | --- |
| D-01 | 森林、太空、外星有不同主题、资源、NPC、活动 | 三张地图配置契约通过；森林入口/节点/活动 URL 已有浏览器部分证据，但活动完成闭环和其余地图手工探索未完成 | partial |
| D-02 | 地图使用统一路线节点和探索会话 | `npm run test:map` 通过 | verified |
| D-03 | 每日路线由到期词、错词和少量新词生成 | plan fixture | planned |
| D-04 | 区域挑战不以 Boss 血量直接代替掌握度 | 规则代码检查 | planned |
| D-05 | 未达标提供补强路线，不清空长期成长 | 手工验收 | planned |

## E. 工程质量

| 编号 | 验收项 | 证据 | 状态 |
| --- | --- | --- | --- |
| E-01 | 每个线程写入 progress 日志并列出命令结果 | 7 个工作流均已写入 `progress/00` 和 `progress/01` | verified |
| E-02 | 线程不修改越权目录 | 各线程范围审计；共享 `package.json` 变更均有日志说明 | partial |
| E-03 | 新增能力有纯函数或契约测试 | CORE/CREEPER/SHOOTER/PINYIN/TENNIS/MAP/ADAPTER 专项测试全部通过 | verified |
| E-04 | Markdown、JSON 和 JS 编码正常 | JSON/JS 解析、语法检查、`git diff --check` 通过 | verified |
| E-05 | 全局 `npm test`、构建和静态服务检查通过 | 根项目 `npm test` 通过；卡片系统 `127/127` 与 contract `12/12` 通过；TENNIS 已有真实浏览器截图，其他游戏完整浏览器走查和真实跨项目接线仍未完成 | partial |
