# 苦力怕当前状态

```text
status: review
current_task: CREEPER-P1-06 验收工具与可观测性补强
owner_thread: CREEPER
last_verified_at: 2026-07-27 13:52:51 +08:00
mode: independent-and-host
```

## 当前判断

- 苦力怕最适合承担英语逐字拼写证据。
- 先完成独立模式学习闭环，再接宿主，不引入章节或 FSRS。
- CREEPER-P1-01 已完成：现状状态机和学习事件映射已写入执行日志。
- CREEPER-P1-02 已完成：标准字段消费、独立/host 最小入口分流和宿主卡片优先已接入。
- CREEPER-P1-03 代码已完成：错误标签、可理解反馈、提示记录、原题重试、换题/超时语义和安全发音降级已接入；错误反馈不写入分数或掌握度。
- CREEPER-P1-04 代码/纯逻辑已完成：结果页展示提示次数和首答率；独立单词模式可按本局 `mistakeCardIds` 启动最多 6 张复习任务，保持错误顺序、去重并跳过不存在的卡片；复习局不触发自动年级升级。
- CREEPER-P1-05 fixture/契约层已完成：`core-v1-fixture.mjs` 提供明确标记为 fixture-only 的 CORE v1 生命周期模型；专项测试覆盖 `ready/init/card-result/complete/stop/error`、重复结果、stop cleanup、独立/host 分流和错误 session 拒绝。
- CREEPER-P1-06 工程修复已完成但浏览器验收未闭环：当前静态服务将 `.mjs` 返回为 `application/octet-stream`，因此浏览器入口依赖改为仓库根 `type: module` 下的 `.js` 文件；`verify.mjs` 改为检查当前 `games/typing-defense/assets/generated` manifest/资源，并显式输出旧生成流水线不可用；`game.js` 的测试模式增加初始化阶段诊断，不进入生产模式。
- 宿主页面仍只消费 draft-v1 `init` fixture；真实 `ready/init/card-result/complete/stop` 生命周期未接入，不能把本阶段 fixture 通过当作真实宿主通过。

## 阻塞

专项浏览器回归仍为 capability-blocked/review。代码修复后的本地证据为：`node games/typing-defense/web/verify-contract.test.mjs`、5 项 CREEPER 专项测试、6 个 `node --check`、`node games/typing-defense/web/verify.mjs` 和 `npm test` 均退出 0；verify 输出 `current resources passed: manifest/assets/page contracts`，并明确警告 `[legacy pipeline unavailable]`；`node games/typing-defense/web/simulate.mjs` 退出 1，原因为 `ERR_MODULE_NOT_FOUND: Cannot find package 'playwright'`。

真实浏览器只做了一次新的 origin/缓存隔离复核：`http://127.0.0.1:7004/games/typing-defense/web/index.html?test=1` 页面加载完成，最终读取到 `bodyTestMode=1`、`bodyUiState=menu`，但 `window.__typingDefenseTest` 仍为未暴露，测试诊断对象也未形成可用证据；该尝试随后按主控指令终止 waiting，不继续重试。未完成结果页本局错词去重/最多 6 张、复习、再玩、返回入口、提示/重试及 360x800 移动端无遮挡验收。Node、fixture、HTTP/MIME、静态 DOM 或页面加载均不得升级为完整浏览器通过；真实宿主 `ready/init/card-result/complete/stop` 仍未接线。
