# 苦力怕执行日志

## 日志格式

```text
时间：
任务：
动作：
命令：
结果：
验收项：
风险/下一步：
```

## 日志

### 2026-07-27

时间：2026-07-27
任务：CREEPER-P1-01；独立模式学习闭环基础能力
动作：读取主控计划、苦力怕 plan/task/验收/progress 和优化方案；盘点状态机、输入、命中/失败、结果页、宿主事件与本地存储；新增独立学习事件纯逻辑和页面接入。
命令：`node web/learning-loop.test.mjs`；`node --check web/game.js`；`node --check web/learning-loop.mjs`；`npm test`
结果：学习专项测试通过；两个脚本语法检查通过；全局契约通过（小游戏 11 个、地图契约通过、完整内容 6324 张卡、完整模式 runtime 通过）。
验收项：CREEPER-A03 部分具备（卡片展示、错误输入、独立/提示后结算、完成耗时和错词可读取）；CREEPER-A07 的学习结果按 `presentationId` 幂等；本地复习记录基础能力已具备。
风险/下一步：`games/typing-defense/web/verify.mjs` 仍因其预期的 `games/typing-defense/tools/` 文件缺失无法启动；`simulate.mjs` 因未安装 `playwright` 无法启动。进入 CREEPER-P1-02，统一标准卡片字段并拆分独立/宿主入口。

## 状态机与事件映射

| 当前行为 | 状态/入口 | 学习事件 | 证据 |
| --- | --- | --- | --- |
| 初始菜单、选择训练模式/词库 | `state=menu` / `renderMenu()` | 无卡片结算 | `game.js:2982` |
| 点击开始，重置局内计分并生成 session | `state=playing` / `startGame()` | 建立本地 session | `game.js:1666`、`learning-loop.mjs` |
| 当前敌人/目标被选中并显示 | `spawnTask()`、`setActiveEnemy()` | `presented`，每个 `presentationId` 一次 | `game.js:1047`、`learning-loop.mjs:158` |
| 输入前缀匹配 | `inputCharacter()` | 无终态，继续输入 | `game.js:2035` |
| 输入字母不匹配 | `inputCharacter()` | `wrong` + `wrong-first-letter` / `wrong-middle-letter` / `wrong-ending` | `game.js:2050` |
| 完整答案命中 | `hitMonster()` | `independent-correct`；若已有错误则归为 `guided-correct` | `game.js:2214`、`learning-loop.mjs:279` |
| 怪物到达终点 | `takeDamage()` | `gave-up` + `slow-completion`，同时沿用原有 `misses`/扣心 | `game.js:2275` |
| 点击“换一个” | `skipCurrent()` | `gave-up`，不能静默丢弃 | `game.js:3043` |
| 本局结束 | `endGame()` | 结束 session，结果页读取本局结算 | `game.js:2376` |

## 当前数据边界

- 局内战斗计数仍是 `hits`、`misses`、`combo`、`hp`；其中 `misses` 只代表怪物造成的失败，输入错误由学习存储单独记录。
- 学习存储键为 `petbank.typing-defense.learning.v1`，记录卡片级 `presented`、`wrong`、`independentCorrect`、`guidedCorrect`、`gaveUp`、错误标签和 `nextReviewAt`。
- 本地记录不依赖 `host/`、`bridge.js` 或卡片系统；`localStorage` 不可用时学习模块会退化为内存记录，不能阻塞当前游戏。
- 当前独立页面仍沿用既有直接输入展示；“看中文/图片主动回忆”、提示控件和错词复习按钮仍留给 P1-03/P1-04；真实宿主 `card-result/complete/stop` 仍留给 P1-05。

### 2026-07-27：CREEPER-P1-02

时间：2026-07-27
任务：CREEPER-P1-02；标准卡片字段与独立/宿主双模式最小入口
动作：按 CORE draft-v1 消费 `init.payload.card` 或 `init.payload.cards`；保留稳定 `cardId`，补齐 `domain/contentType` 默认值；无有效 host 卡时继续使用本地词库，有效 host 卡优先于自动词库；不把游戏 `score` 写入学习记录。
命令：`node web/card-runtime.test.mjs`；`node web/learning-loop.test.mjs`；`node --check web/game.js`；`node --check web/card-runtime.mjs`；`node --check web/learning-loop.mjs`；`node --check web/simulate.mjs`；`npm test`
结果：卡片适配专项通过；学习专项通过；四个脚本语法检查通过；全局契约通过。`simulate.mjs` 增加独立入口无卡片、host fixture 优先和标准字段断言，但当前无法执行浏览器。
验收项：独立入口不传卡片仍使用 local；host payload 卡片优先；当前任务可读取 `cardId/domain/contentType`；学习记录只由事件结算，`score` 不产生 mastery 字段。
风险/下一步：`verify.mjs` 仍因预期的 `games/typing-defense/tools/build_typing_tasks_from_vocab.cjs` 缺失无法启动；`simulate.mjs` 仍因未安装 `playwright` 无法启动。已消费静态 draft fixture，但没有实现或宣称真实宿主生命周期；下一步进入 CREEPER-P1-03。

### 2026-07-27：CREEPER-P1-03

时间：2026-07-27
任务：CREEPER-P1-03；错误标签、提示/重试、换题/超时反馈与发音降级
动作：先补红测并确认反馈模块缺失、学习层缺少 `recordHint`；新增 `feedback-runtime.mjs` 纯逻辑反馈映射和 `speakTextSafely`，学习层记录 hint 并使最终结果归类为 `guided-correct`；页面接入错误/提示/成功/换题/超时可理解反馈、原题低难度重试、gave-up 语义和 SpeechSynthesis 安全降级；不把 score 写入学习记录。按最新指令停止继续扩大 `simulate.mjs` 页面级改动。
命令：`node web/feedback-runtime.test.mjs`；`node web/learning-loop.test.mjs`；`node web/card-runtime.test.mjs`；`node --check web/game.js`；`node --check web/feedback-runtime.mjs`；`node --check web/learning-loop.mjs`；`node --check web/simulate.mjs`；`npm test`；`node web/verify.mjs`；`node web/simulate.mjs`；`git diff --check`
结果：反馈专项、学习专项、卡片专项均通过；相关语法检查通过；`npm test` 通过（小游戏契约 11 个、地图/森林/探索契约、完整内容 6324 张卡、完整模式 runtime）；`git diff --check` 退出 0。`verify.mjs` 因缺少 `tools/build_typing_tasks_from_vocab.cjs` 退出 1；`simulate.mjs` 因未安装 `playwright` 退出 1。
验收项：首字母/中间/结尾错误均映射为可理解标签并显示答案；提示可读取 `hintUsed`，提示后正确记录为 `guided-correct`；原题重试清空当前输入并降低当前进度；换题记录 `gave-up`；超时记录 `slow-completion`；SpeechSynthesis 缺失或抛错返回安全降级；未新增 mastery 或 score 到学习记录。
风险/下一步：P1-03 纯逻辑/代码完成，但浏览器验证 blocked，不能宣称 CREEPER-A01/A04/A05/A08 或真实宿主生命周期已通过；下一步 CREEPER-P1-04，恢复浏览器依赖后补页面回归。

### 2026-07-27：CREEPER-P1-04

时间：2026-07-27 09:42:10 +08:00
任务：CREEPER-P1-04；独立模式错词复习闭环与结果页证据
动作：先新增红测定义独立复习队列和结果摘要，再实现 `independent-loop.mjs`；结果页接入提示次数、首答正确率和“复习本局错词”动作。复习任务按当前 session 的 `mistakeCardIds` 保持事件顺序、去重、最多 6 张并跳过当前词库不存在的卡片；独立复习局复用本地词库，跳过自动年级升级；宿主模式不显示本地错词复习动作。
修改文件：`games/typing-defense/web/independent-loop.mjs`、`games/typing-defense/web/independent-loop.test.mjs`、`games/typing-defense/web/game.js`；更新本线程 `task/01-任务清单.md`、`progress/00-当前状态.md`、`progress/01-执行日志.md`；未修改 `host/`、`bridge.js`、`games/learning-arcade/`、卡片系统或其他工作流文件。
命令：`node web/independent-loop.test.mjs`；`node web/learning-loop.test.mjs`；`node web/feedback-runtime.test.mjs`；`node web/card-runtime.test.mjs`；`node --check web/game.js`；`node --check web/independent-loop.mjs`；`node --check web/independent-loop.test.mjs`；`git diff --check -- games/typing-defense/web`；`npm test`；`node web/verify.mjs`；`node web/simulate.mjs`
结果：四项苦力怕专项测试通过；三个脚本语法检查通过；`git diff --check` 通过；全局 `npm test` 通过（小游戏 11 个、地图/森林/探索契约、完整内容 6324 张卡、完整模式 runtime）。`verify.mjs` 退出 1，原因是缺少 `games/typing-defense/tools/build_typing_tasks_from_vocab.cjs`；`simulate.mjs` 退出 1，原因是未安装 `playwright`。
验收项：独立结果页现在具备本局错词出口和提示/首答证据；复习队列纯逻辑通过，复习模式保持独立本地词库并限制 6 张；宿主卡片入口未改变。未宣称 CREEPER-A01/A04/A05/A06/A08 的浏览器通过，也未宣称 CREEPER-A02/A07 的真实宿主生命周期完成。
风险/下一步：P1-04 保持 `review`，等待浏览器依赖恢复后验证结果页点击、复习任务确实只来自本局错词及移动端布局；P1-05 仍待真实 `ready/init/card-result/complete/stop` 回归，期间不扩展共享宿主代码。

### 2026-07-27：CREEPER-P1-05

时间：2026-07-27 10:07:44 +08:00
任务：CREEPER-P1-05；CORE v1 fixture/契约层与宿主分流回归
动作：先新增红测确认 fixture 模块缺失，再新增 `games/typing-defense/web/core-v1-fixture.mjs`；fixture 复用 `card-runtime.mjs` 的标准卡片归一化，覆盖独立本地卡、合法 host `init`、错误 session 拒绝、每卡单次 `card-result`、`complete`/`stop` 终态、`error` 和一次性 cleanup；明确标记 `fixtureOnly`，不引入 `host/`、`bridge.js`、`games/learning-arcade/` 或卡片系统改动。
命令：`node games/typing-defense/web/core-v1-fixture.test.mjs`（先红后绿）；`node games/typing-defense/web/card-runtime.test.mjs`；`node games/typing-defense/web/feedback-runtime.test.mjs`；`node games/typing-defense/web/learning-loop.test.mjs`；`node games/typing-defense/web/independent-loop.test.mjs`；相关 `node --check`；`npm test`；`git diff --check -- games/typing-defense/web`；`node games/typing-defense/web/verify.mjs`；`node games/typing-defense/web/simulate.mjs`
结果：CORE v1 fixture 专项及既有四项苦力怕专项通过；相关脚本语法检查通过；`npm test` 通过（小游戏 11 个、地图/森林/探索、完整内容 6324 张卡、完整模式 runtime）；`git diff --check` 无错误。`verify.mjs` 因缺少 `tools/build_typing_tasks_from_vocab.cjs` 退出 1；`simulate.mjs` 因未安装 `playwright` 退出 1。
验收项：P1-05 fixture/契约层覆盖 `ready/init/card-result/complete/stop/error`、重复结果、stop cleanup、独立/host 分流；初始化卡片和 host 卡片标准字段可读取，host 卡片不会被本地卡替换；本阶段不宣称真实浏览器宿主生命周期。
风险/下一步：真实宿主 `ready/init/card-result/complete/stop` 接线仍需共享桥接/宿主所有者后续确认；浏览器页面验证等待旧 verify 资源和 Playwright 依赖恢复，当前不得修改共享宿主代码补偿。

复核：2026-07-27 10:16:35 +08:00；严格确认 `ready` 的 CORE payload 为 `{}`，`error` 在 ready 前被拒绝；红测先失败后修正通过。最终苦力怕专项 5 项、相关语法、`npm test` 和 `git diff --check` 均通过；`verify.mjs` 仍缺少 `tools/build_typing_tasks_from_vocab.cjs`，`simulate.mjs` 仍缺少 `playwright`。

### 2026-07-27：CREEPER-P1-04 浏览器验收收口

时间：2026-07-27
任务：CREEPER-P1-04；独立错词复习/结果页真实浏览器验收
动作：遵循主控纠偏，仅进行一次有界浏览器重试；首次初始化超时后，重试使用当前可用的 Codex 应用内浏览器打开本地页面并尝试开始游戏；未重试浏览器初始化，不改造旧 verify/Playwright 工具。
命令：`node games/typing-defense/web/verify.mjs`；`node games/typing-defense/web/simulate.mjs`；真实浏览器打开 `http://127.0.0.1:7003/games/typing-defense/web/index.html?test=1`；定位并点击 `#overlayStart`；读取 `window.__typingDefenseTest.snapshot()`；尝试保存 `C:\tmp\creeper-p1-04-desktop-initial.png`
结果：`verify.mjs` 退出 1，原因是缺少 `games/typing-defense/tools/build_typing_tasks_from_vocab.cjs`；`simulate.mjs` 退出 1，原因是未安装 `playwright`。有界重试成功完成浏览器连接、页面加载和 `#overlayStart` 点击，初始桌面截图已捕获到浏览器工具输出，但文件保存返回 `EPERM`，未落盘。页面未暴露 `window.__typingDefenseTest`，读取快照报 `TypeError: Cannot read properties of undefined (reading 'snapshot')`；页面仍停留在菜单/开始覆盖层，URL 未变化，未见浏览器控制台错误/警告。
验收项：仅取得页面加载级真实浏览器证据；未取得结果页本局错词去重/限 6、复习入口、再玩、返回入口、提示/重试语义或 360x800 移动端无主要遮挡的真实浏览器证据。Node 专项、fixture、HTTP/页面加载证据不能替代这些验收；真实宿主 `ready/init/card-result/complete/stop` 仍未接线。
风险/下一步：P1-04 保持 `review`，并记录为浏览器能力阻塞；等待项目可用的浏览器测试能力、缺失的 verify 资源和 Playwright 依赖恢复后再做一次完整页面验收。当前回合结束，不修改 `host/`、`bridge.js`、`games/learning-arcade/`、PINYIN、卡片系统或共享工具。

### 2026-07-27：CREEPER-P1-06 验收工具与可观测性补强收口

时间：2026-07-27 13:52:51 +08:00
任务：CREEPER-P1-06；修复当前验收链路并补测试模式可观测性，不扩展玩法、不接宿主
动作：先读取实施计划、验收矩阵、当前 task/progress/log、`verify.mjs`、`simulate.mjs`、`index.html`、`game.js` 和当前资源目录；复现 `?test=1` 下 `window.__typingDefenseTest` 未暴露。HTTP 诊断确认 `game.js` 为 `text/javascript`，四个被入口导入的 `.mjs` 返回 `application/octet-stream`，而历史 `games/typing-defense/tools`、`prompts` 和旧素材源均不存在。将浏览器运行时的四个 ESM 文件改为 `.js` 并更新苦力怕内部引用；`verify.mjs` 改为基于当前生成 manifest/资源检查，旧生成流水线仅以显式 `legacy pipeline unavailable` 警告报告；新增 `verify-contract.test.mjs`，并在 `?test=1` 的 `game.js` 初始化阶段增加错误/未处理拒绝诊断。
修改文件：`games/typing-defense/web/game.js`、`games/typing-defense/web/card-runtime.js`、`games/typing-defense/web/feedback-runtime.js`、`games/typing-defense/web/learning-loop.js`、`games/typing-defense/web/independent-loop.js`、`games/typing-defense/web/card-runtime.test.mjs`、`games/typing-defense/web/feedback-runtime.test.mjs`、`games/typing-defense/web/learning-loop.test.mjs`、`games/typing-defense/web/independent-loop.test.mjs`、`games/typing-defense/web/core-v1-fixture.mjs`、`games/typing-defense/web/verify.mjs`、`games/typing-defense/web/verify-contract.test.mjs`；原浏览器运行时 `.mjs` 路径相应改名为 `.js`。未修改 `simulate.mjs`、`host/`、`bridge.js`、`games/learning-arcade/`、PINYIN、TENNIS、MAP、卡片系统或全局测试文件。
命令：`node games/typing-defense/web/verify-contract.test.mjs`（0）；`node games/typing-defense/web/card-runtime.test.mjs`（0）；`node games/typing-defense/web/feedback-runtime.test.mjs`（0）；`node games/typing-defense/web/learning-loop.test.mjs`（0）；`node games/typing-defense/web/independent-loop.test.mjs`（0）；`node games/typing-defense/web/core-v1-fixture.test.mjs`（0）；`node --check games/typing-defense/web/game.js`、`card-runtime.js`、`feedback-runtime.js`、`learning-loop.js`、`independent-loop.js`、`verify.mjs`（均 0）；`node games/typing-defense/web/verify.mjs`（0）；`npm test`（0）；`node games/typing-defense/web/simulate.mjs`（1）。
结果：verify-contract 3 个测试通过；5 项 CREEPER 专项均通过；语法检查均通过；`verify.mjs` 输出当前资源检查通过，并报告旧流水线缺失；全局 `npm test` 通过。`simulate.mjs` 保持 capability-blocked，精确错误为 `ERR_MODULE_NOT_FOUND: Cannot find package 'playwright'`，未安装、补造或假设 Playwright。
浏览器证据边界：修复后只做一次新的 `127.0.0.1:7004` origin/缓存隔离复核，页面完成加载并可读 `bodyTestMode=1`、`bodyUiState=menu`；`window.__typingDefenseTest` 仍未暴露，测试诊断对象也未形成可用证据，随后遇到 waitingOnApproval/等待能力限制并终止。没有结果页、复习、再玩、返回入口、提示/重试或 360x800 移动端真实浏览器证据；不把该页面加载、HTTP/MIME、Node、fixture 或静态 DOM 证据当作浏览器通过，也不宣称真实宿主 CORE 生命周期完成。
验收项：CREEPER-P1-06 保持 `review/capability-blocked`。代码修复和本地契约证据已完成；浏览器/Playwright 能力仍阻塞，P1-04 的结果页和移动端验收未完成，真实 `ready/init/card-result/complete/stop` 仍由宿主所有者后续接线确认。
