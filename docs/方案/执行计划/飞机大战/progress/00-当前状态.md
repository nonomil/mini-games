# 飞机大战当前状态

```text
status: review
current_task: SHOOTER-P1-04
owner_thread: SHOOTER
last_verified_at: 2026-07-27T13:04:25+08:00
shared_file_owner: games/learning-arcade/game.js
```

## SHOOTER-P1-01 边界盘点证据

| 领域 | `word-shooter` 当前实现 | `word-cannon` 当前实现 | PINYIN 交接规则 |
| --- | --- | --- | --- |
| 状态 | `state.wordShooter` 持有敌机、战机、输入缓冲、护盾、武器、学习统计、计时器和结果；`state.activeGame` 只用于生命周期路由 | `state.wordCannon` 持有拼音目标、车辆、地图段、学习统计、计时器和结果；不读取英语输入缓冲 | 只能消费快照和公开方法，不读取 `state`、DOM 节点或英语词卡状态 |
| 输入 | 委托 `keydown/keyup`：方向键写入 `moveInput`，单个 `a-z` 写入 `inputWordLetter`；字母 `event.repeat` 不重复提交；屏幕字母键点击后恢复 `gameScreen` 焦点 | `Arrow`/`WASD` 经过 `inputWordCannonLetter` 驱动车辆；拼音屏幕键只提供左右控制；不复用英语输入缓冲 | PINYIN 不依赖共享键盘副作用；需要新按键或入口时必须先由 SHOOTER 增加契约测试并评审 |
| 渲染 | `renderTypingArena()` 统筹英语 HUD、单词队列、敌机、战机、弹幕、危险物、Boss、掉落和英语键盘 | `renderWordCannonStage()` 统筹赛车路线、拼音目标、车辆、反馈、方向控制和拼音键盘 | 只通过 `wordCannon()` 读取结果；不调用任一内部 renderer |
| 卡片选择 | `ensureGameData('word-shooter')` -> `ensureTypingData()` -> `state.words` -> `wordsForDifficulty()` -> `wordShooter.roundWords`/敌机 `wordData` | `ensureGameData('word-cannon')` -> `ensureHanziData()` -> `state.hanzi` -> `pinyinCannonItemAt()` -> 拼音赛车 wave/target | 英语词卡与拼音题卡完全分域；PINYIN 只能使用汉字/拼音数据边界 |
| 生命周期/计时 | `openGame('word-shooter')` 负责停旧局、加载英语数据、启动 `startWordShooter()`；`stopWordShooter()` 清理 interval；`tickWordShooterFrame()` 供测试推进 | `openGame('word-cannon')` 负责停旧局、加载汉字数据、启动 `startWordCannon()`；`stopWordCannon()` 清理 interval；`tickWordCannonFrame()` 供测试推进 | 共享生命周期只认 `openGame` 和 `state.activeGame` 的公开行为，不新增并行 timer |

### PINYIN 当前稳定入口

PINYIN 现在可以开始修改自己的专属模块和 fixture，并可调用以下现有入口：

- `window.LearningArcadePrototype.openGame('word-cannon')`
- `window.LearningArcadePrototype.wordCannon()`
- `window.LearningArcadePrototype.selectWordCannonMap(mapIndex)`
- `window.LearningArcadePrototype.tickWordCannonFrame(stepMs, frames)`
- `window.LearningArcadePrototype.hanziPool()` 和 `hanziPack()`

PINYIN 不得修改或依赖 `games/learning-arcade/game.js` 的内部函数、`state.wordShooter`、`state.input`、`state.combo`、英语词库、DOM id 或 localStorage key。PINYIN 没有直接修改共享入口的权限；若专属实现需要新增共享字段/API，由 PINYIN 提交需求和 fixture，由 SHOOTER 代改 `game.js`，并先补充 `tests/test-shooter-runtime.mjs` 契约、经主控确认后再合入。

交接条件：`SHOOTER-P1-01` 已为 `verified`，专项测试、语法检查和 `npm test` 均为退出码 0，因此 PINYIN 可立即开始专属代码；在 `SHOOTER-P1-02` 的真实浏览器长按/移动端方向控制验收完成前，不得依赖任何新的共享键盘行为。

## 已冻结的共享边界

| 领域 | `word-shooter` | `word-cannon` / PINYIN 可依赖边界 |
| --- | --- | --- |
| 状态 | `state.wordShooter` 管理敌机、战机、输入缓冲、学习统计、计时器和局内结果；只通过 `state.activeGame` 共享生命周期 | `state.wordCannon` 管理目标、车辆、拼音任务、地图段、学习统计和计时器；不读取英语输入缓冲 |
| 输入 | `ArrowUp/Down/Left/Right` 只更新 `moveInput`；`a-z` 只进入 `inputWordLetter`；不再按当前单词首字母抢占 WASD | `Arrow` 和 `W/A/S/D` 继续由 `inputWordCannonLetter` 换道/前后移动；屏幕方向键走同一控制函数 |
| 渲染 | `renderTypingArena()` 统筹 `renderWordQueue()`、敌机、战机、HUD 和英语键盘 | `renderWordCannonStage()` 统筹赛道、拼音目标、车辆、反馈和方向键；不复用英语键盘 |
| 卡片选择 | `ensureTypingData()` -> `state.words` -> `wordsForDifficulty()` -> `wordShooter.roundWords` | `ensureHanziData()` -> `state.hanzi`/`state.hanziAll` -> `pinyinCannonItemAt()`；不读取英语词卡答案 |
| 稳定 API | `LearningArcadePrototype.wordShooter()` 仅用于飞机快照 | PINYIN 可调用 `openGame('word-cannon')`、`wordCannon()`、`selectWordCannonMap()`、`tickWordCannonFrame()`、`hanziPool()` 和 `hanziPack()`；新增字段/API 必须先由 SHOOTER 评审 |

PINYIN 可在本边界测试通过并由主控确认 `SHOOTER-P1-01` 为 `verified` 后接入自己的专属模块/fixture；不需要也不得修改 `games/learning-arcade/game.js`。在 `SHOOTER-P1-02` 的浏览器手工验证完成前，PINYIN 不应依赖任何新的键盘副作用。

## P1-02 浏览器验收

时间：2026-07-27T01:27:31+08:00

- 服务命令：复用已运行的 `node local-server.mjs`，`http://127.0.0.1:7003/games/learning-arcade/?game=word-shooter` 返回 `200`；服务脚本与工作区 `game.js` 字符数均为 `229091`，且均包含焦点恢复和 `event.repeat` 防护。
- 页面上下文：标题为 `学习机玩法原型 · 三个小游戏`，脚本 URL 正确，`iframeCount=0`，顶层 DOM 的 `#gameScreen[data-active-game=word-shooter]` 和 `word-shooter` 面板均可见；不是错误页或 iframe 页面。
- 桌面 DOM：真实发送首字母后完成字母数由 `0` 变 `1`，反馈进入“继续输入”；发送 `ArrowRight` 不增加完成字母、不改变输入反馈。字母 `a` 也能进入输入且 `--player-x/--player-y` 保持 `12%/50%`，未触发 WASD 移动。
- 移动端 DOM：`390x844` 下页面无横向溢出，26 个英文按键可见；正确字母点击后完成字母数为 `1`，焦点恢复到 `gameScreen`。当前 SHOOTER 没有方向触控按钮，字母键最小约 `32px`，低于优化方案的 `44px` 触控建议。
- 工具限制：浏览器评估器中 `typeof window.LearningArcadePrototype` 为 `undefined`，同时 `typeof globalThis` 为 `undefined` 且 `window === globalThis` 为 `false`；这是评估器上下文限制，不能据此判断页面脚本未执行。当前浏览器 API 只能发送按下即释放的按键，无法合成真实长按/`event.repeat`，因此这两项不标记为浏览器通过。

结论：P1-02 保持 `review`。代码级 guard、桌面 DOM 输入/方向隔离和移动端焦点恢复已有证据；真实长按、失焦后重新聚焦、移动端方向控制/触控尺寸仍需真实 Chrome 或补充 E2E 能力验证。

## P1-02 本轮补充验收

时间：2026-07-27T10:37:26+08:00

- 真实本地浏览器重新加载 `?game=word-shooter` 后，SHOOTER DOM 出现 `data-shooter-direction` 的上下左右四个按钮；默认视口按钮为 `48x44`，页面无横向溢出。
- `390x844` 视口下四个方向按钮均为 `44x44`，26 个英文键仍可见，页面无横向溢出；方向按钮没有 `data-key`，点击右方向后输入状态不增加，焦点保持在 `gameScreen`。
- `game.js` 仅增加 SHOOTER 动态方向控件和 `pointerdown`/`pointerup`/`pointercancel` 到 `moveInput` 的最小桥接；窗口 `blur` 仍通过既有路径清空所有方向，按键字母 `event.repeat` guard 未改变。
- 浏览器能力阻塞：当前可用控制接口只能发出按下即释放的 click，不能保持 `pointerdown` 到下一帧，也不能稳定合成真实键盘长按/`event.repeat` 或切换窗口失焦；单次方向 click 的 `playerX` 未变化，因此不把持续移动、长按/repeat 或失焦恢复升级为浏览器通过。页面仍可正常加载，工具侧 Statsig 网络超时不影响本地 DOM 结果。
- 新鲜回归：`node tests/test-shooter-runtime.mjs`、`node --check games/learning-arcade/game.js`、`npm test` 和 `git diff --check -- games/learning-arcade/game.js tests/test-shooter-runtime.mjs` 均退出码 0。

结论：P1-02 仍保持 `review`。移动端方向控件与触控尺寸已补齐并有真实 DOM/点击证据；真实长按/repeat、持续移动和失焦恢复仍 blocked，不能标记 `verified`。

## P1-02 本线程能力复核

时间：2026-07-27T10:44:51+08:00

- 重新连接本地真实浏览器并打开 `http://127.0.0.1:7003/games/learning-arcade/?game=word-shooter`；页面标题、`#gameScreen[data-active-game=word-shooter]` 和四个 `data-shooter-direction` 按钮均正常。
- 默认视口下方向按钮均为 `48x44`，页面无横向溢出；按钮没有 `data-key`。真实 click 前后 `typingProgress`、当前字母和 `gameScreen` 焦点均不变，确认方向 click 不写入字母输入。
- 能力阻塞证据：当前浏览器动作面只提供 `click`/`keypress` 等成对动作，没有可保持的 `pointerdown`、`pointerup`、`keydown` 或 `keyup` 原语；因此本轮无法合成长按、键盘 `event.repeat`、持续移动或窗口失焦/恢复，不能将这些结果升级为浏览器通过。

结论：P1-02 继续保持 `review`；不进入 P1-03。已有 `390x844` 真实浏览器记录仍有效，四方向按钮为 `44x44` 且无横向溢出；待获得可保持按下和可切换失焦的真实 Chrome/E2E 能力后再补验。

## P1-03 主动输入阶段实现与验收

时间：2026-07-27T12:31:24+08:00

- `WORD_DIFFICULTY_TUNING` 将飞机大战目标数收束为基础/进阶/完整 `6/7/8`，每局通过 `uniqueWordShooterRoundWords()` 选择唯一词卡；`nextWordShooterWord()` 排除已占用、已解决和重复首字母目标，`recordWordShooterTargetResult()` 以 `targetId`/单词去重，保证逐卡最多一次结果。
- 每个敌机快照包含 `prompt.translation`、`prompt.image`、`prompt.audio`、`operationWindowMs`、`deadlineAt`；操作窗口只影响 `slow-completion` 判定，学习统计不读取或改写掌握度/Profile/章节解锁。
- 目标结果覆盖 `completed`/`missed`、`completionMs`、错误标签和结果时间；输入首字符错误记录 `first-character-error`，中途拼写错误记录 `spelling-error`，超出窗口完成记录 `slow-completion`，撞线记录 `missed-target`。漏目标移除后继续补充下一目标，基础局不会因一次漏目标立即结束。
- `roundStatus`、`paused`、`targetResults`、错误标签和学习统计进入 `wordShooter()` 快照；暂停按钮冻结更新并清空移动输入，停止/返回主页/再玩清理 interval 和四方向 `moveInput`。结果页动态提供复习错题、逐题听音、再来一局和返回主页入口。
- 真实浏览器页面确认：基础训练显示 `6` 目标、中文/图片提示、下一目标和四方向按钮；错误首字母提示可见；输入 `grass` 后完成目标并显示 `slow-completion` 反馈；暂停/恢复按钮状态可见；结果页真实显示复习错题/再来一局/返回主页，复习面板列出 `grass` 及 `first-character-error、slow-completion`；`390x844` 下四方向按钮均为 `44x44`、无横向溢出且不带 `data-key`。
- 浏览器限制：当前页面评估上下文中的 `window.LearningArcadePrototype` 不可读，因此没有把 DOM 现象升级为内部 `targetResults`/完成 6-8 目标数据通过；动作面仍不能稳定验证真实长按、`event.repeat`、持续移动或失焦恢复。P1-02 保持 `review`，P1-03 也保持 `review`。

## SHOOTER-P1-04 映射收口与实现

时间：2026-07-27T12:42:56+08:00

P1-04 原验收定义保留为“错误、减速、重试和结果复习”，本轮仅核对 P1-03 的覆盖范围：

| P1-04 能力 | P1-03 证据 | 收口结论 |
| --- | --- | --- |
| 错误 | `first-character-error`、`spelling-error`、`missed-target`、`slow-completion` 已进入目标结果、学习统计、反馈和摘要 | 已被 P1-03 覆盖 |
| 减速 | `operationWindowMs`/`deadlineAt` 只改变操作窗口并记录慢完成；漏目标后当前实现不会暂停或减速 | 部分覆盖；漏目标后的暂停/减速仍是差异 |
| 重试 | 错误输入可继续当前目标并计入 `retryWords`；结果页支持复习听音和整局再玩，但没有漏目标后的逐卡重试入口 | 部分覆盖；逐卡重试仍是差异 |
| 结果复习 | 结果页提供复习错题、逐题听音、再来一局和返回主页，结果快照保留目标与错误数据 | 已被 P1-03 覆盖 |

映射阶段确认的差异已在本轮实现，P1-04 状态保持 `review` 而不是 `verified`。另一个原计划差异“连续错误降低题型难度、不直接提高速度”也已通过局内辅助等级补齐，不修改全局难度、掌握度、Profile 或章节解锁。

时间：2026-07-27T13:04:25+08:00

- `retryState` 提供 `idle`/`retryable` 状态；目标撞线或 `deadlineAt` 到期后移出战场并暂停，展示答案、`重试这一卡` 和 `跳过并复习`。重试复用原 enemy/card id、刷新该卡和其他在场目标的操作窗口；跳过才调用 `recordWordShooterTargetResult(..., 'missed')`，因此每卡最多一个结果。
- `wordShooterDifficultyAdjustment()` 只读取局内 `learning.difficultyAssistLevel`；连续两次输入错误后提高辅助等级，后续目标降低速度并延长操作窗口，正确输入会清除连续错误计数。没有写入 `state.wordDifficulty`、Profile、掌握度或章节解锁。
- `wordShooter()` 和结果摘要新增 retry/skip/辅助等级统计；停止、返回主页、再玩和 retry/skip 迁移均清空四方向 `moveInput`，结果页继续保留复习、听音、再玩和返回主页出口。
- 真实浏览器观察：等待操作窗口后真实出现“等待选择”、答案、重试/跳过按钮；点击重试后 retry 面板消失、原卡重新出现并显示“这张卡只记录一个最终结果”；点击跳过后训练继续并显示答案进入复习；连续两次错误显示“后续目标会放慢并延长操作窗口”。浏览器评估上下文仍不能读取内部结果对象，长按/repeat、持续移动和失焦恢复仍 capability blocked。
- 回归：`node tests/test-shooter-runtime.mjs`、`node --check games/learning-arcade/game.js`、`npm test` 均通过；`git diff --check` 退出码 0，仅有既有 CRLF 警告。

## P1-05 前置条件

- P1-01 共享边界保持 `verified`；P1-02、P1-03 的真实浏览器能力项仍保持 `review`，长按/repeat、持续移动、失焦恢复和内部结果对象不能用 DOM/Node/HTTP 证据替代。
- 需要真实宿主结果 fixture 验证飞机模式完成/失败结果能通过现有公开边界传出；不得为此修改 `host/bridge.js` 或新增共享内部依赖。
- PINYIN 只能调用既有 `word-cannon`/`hanziPool`/`hanziPack` 等公开入口，专属 fixture 不得读取 `state.wordShooter`、英语卡片或 DOM，也不得改 `game.js`。
- 满足上述前置条件并由主控确认后，才开始 P1-05 宿主结果和 PINYIN 边界回归；P1-04 已实现但仍 review，当前不启动 P1-05。

## P1-03 回归证据

- `node tests/test-shooter-runtime.mjs`：通过，输出 `PASS shooter runtime boundary and keyboard contract`。
- `node --check games/learning-arcade/game.js`：通过。
- `npm test`：通过，包含 `mini games contract passed: 11 games`、`PASS map contract: 3 maps`、`PASS forest vertical slice contract`、`PASS exploration session contract`、`PASS complete content contract: 6324 cards`、`PASS complete mode runtime contract`。
- `git diff --check`：退出码 0；仅有工作区既有文件的 LF/CRLF 警告，无空白错误。

## 关键风险

- 拼音赛车当前与飞机大战共用 `game.js`，本线程必须先交付共享边界。
- 输入难度不能用单纯加速代替学习难度。
- `W/A/S/D` 不再是飞机大战默认移动键；如未来恢复可选 WASD，必须增加显式设置并保持输入优先级稳定。

## 阻塞

- 浏览器能力阻塞（非代码阻塞）：当前控制接口没有可保持的按下/释放原语，无法完成真实长按/repeat、持续移动和窗口失焦/恢复验证；获得真实 Chrome 或补充 E2E 能力后再补验。
- 若发现需要改 CORE 文件，记录请求并交回 CORE，不自行修改。
