# 飞机大战任务清单

| ID | 任务 | 文件范围 | 依赖 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| SHOOTER-P1-01 | 盘点并冻结 learning-arcade 共享边界 | `games/learning-arcade/game.js`、专项测试、progress | 无 | verified | `node tests/test-shooter-runtime.mjs`、`node --check games/learning-arcade/game.js`、`npm test`；共享状态、输入、渲染、卡片选择、生命周期和 PINYIN API 边界已记录 |
| SHOOTER-P1-02 | 分离移动键和字母输入 | `game.js`、专项测试 | P1-01 | review | 真实浏览器已确认四方向触控按钮存在且 `390x844` 下均为 `44x44`、无横向溢出、方向按钮不写入字母并恢复 `gameScreen` 焦点；`pointerdown`/`pointerup`/`pointercancel` 已接入 `moveInput`。本轮复核确认动作面只有成对 `click`/`keypress`，不能合成长按/`event.repeat`、失焦或持续移动，保持 `review` |
| SHOOTER-P1-03 | 建立 6-8 目标主动输入阶段 | `game.js`、专项测试、progress | P1-02 | review | 基础/进阶/完整局为 6/7/8 个唯一目标；每卡最多记录一次 `targetResults`，含中文/图片/听音提示、操作窗口和耗时；覆盖 `missed-target`、`slow-completion`、`first-character-error`、`spelling-error`；漏目标继续下一卡；暂停/停止清理移动输入；完成页提供复习错题、再玩、返回主页。专项测试、语法检查、全局 `npm test` 和 `git diff --check` 通过。真实浏览器已确认提示、暂停/恢复、错误反馈、结果复习入口和 `390x844` 触控尺寸；浏览器上下文不能读取内部运行时结果，且长按/repeat/失焦能力仍 blocked，保持 review |
| SHOOTER-P1-04 | 错误、减速、重试和结果复习 | `game.js`、入口页面、测试 | P1-03 | review | 已实现 retry/skip 逐卡状态：漏目标/窗口到期进入暂停 retry，答案、重试和跳过可见；重试复用原卡、跳过提交 missed 结果；连续两次错误提升局内辅助等级，降低后续目标速度并延长操作窗口；结果快照/摘要保留重试、跳过和辅助统计，既有复习/再玩/返回出口保留。每卡结果继续由 `targetResults` 去重。真实浏览器已确认 retry、skip 和连续错误辅助反馈；内部结果对象及长按/repeat/持续移动/失焦仍 blocked，保持 review。 |
| SHOOTER-P1-05 | 宿主结果和 PINYIN 边界回归 | 专项测试、progress | CORE verified | planned | - |

## 禁止越界

- 不修改苦力怕目录。
- 不直接实现拼音字段或拼音判分。
- 不用 `score/combo` 直接更新卡片掌握度。
- 不删除 `word-cannon` 现有入口，除非主控批准迁移方案。
