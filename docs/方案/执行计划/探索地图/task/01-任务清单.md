# 探索地图任务清单

| ID | 任务 | 文件范围 | 依赖 | 状态 | 证据 |
| --- | --- | --- | --- | --- | --- |
| MAP-P0-01 | 统一路线节点和地图 manifest | `data/maps-manifest.json`、地图目录 | 无 | done | `npm run test:map`：3 张地图、统一节点字段通过 |
| MAP-P0-02 | 探索会话、活动返回和恢复 | `games/explore-map/`、`games/word-memory-map/`、测试 | CORE draft | done | `npm run test:map`：保存/恢复、损坏容错、returnContext 编解码通过 |
| MAP-P1-01 | 森林 RPG 垂直切片 | `games/explore-map/`、`games/word-memory-map/`、地图专项测试 | P0 | browser-partial-blocked | 真实浏览器第二次完成 `50/50`，完成页 `#returnToExploreButton` 鼠标点击成功返回；原 `mapId=forest-farm`、`routeId=forest-farm-main-v1`、`nodeId/currentNodeId=forest-explorer-03`、`taskId=forest-word-explorer` 和 `completedNodeIds=[forest-entry,forest-guide-01]` 保留。总体仍为 review：桌面地图 `scrollWidth=1286/clientWidth=1265`，历史 360x800 实测 `375/345` 约 30px 溢出，本回合浏览器绑定无法调整到 360x800 复测。历史 `6/50` 记录保留在执行日志 |
| MAP-P1-02 | 消除地图横溢出和移动布局约束 | `games/explore-map/index.html` 局部 `<style>`、`tests/test-forest-vertical-slice.mjs`、MAP 自身记录 | P1-01 | review | 已按主控授权增加 `#page-explore`、`#pixelStoryMapHost` 的收缩/横溢出约束和 `.pixel-story-shell` 的 `border-box/max-width`；专项测试、全局回归、语法检查和 diff 检查通过。真实浏览器桌面 `1280x720` 测得 document/body `1265/1265`，shell `1240px`；真实 `360x800` 测得 document/body `345/345`，shell `345px`，两种视口均无横溢出。未修改全局 `vendor/css/pixel-story.css`、节点逻辑或 `returnContext`。历史 scope 冲突已解除，等待主控复核 |
| MAP-P2-01 | 太空/外星资源和路线差异 | 地图配置、活动桥 | P1 | planned | - |
| MAP-P2-02 | 每日路线和区域挑战 | 任务配置、测试 | P0/P1 | planned | - |
| MAP-P3-01 | 语法塔/天空扩展入口 | manifest、文档 | 内容包前置 | planned | - |

## 地图线程不得做的事

- 不直接写 Profile、FSRS、XP 或卡片掌握度。
- 不在地图里重新随机一套词库替代宿主 session。
- 不把四个小游戏强制串成一次地图流程。
- 不把 Boss 失败变成清空长期资源。
