# 探索地图当前状态

```text
status: review
current_task: MAP-P1-02（review，局部修复与真实桌面/360x800 几何已验证，等待主控复核）
owner_thread: MAP
last_verified_at: 2026-07-27
maps: forest-farm, space-farm, alien-farm
```

## 当前判断

- RPG 参考文档用于指导探索地图包装，不新增独立题库或独立成长系统。
- `Daily Dungeon` 对应每日探索路线，`Boss Challenge` 对应区域检查点，`Grammar Castle` 仅预留内容建筑。
- 三张主地图已在 `data/maps-manifest.json` 建立统一路线节点契约：`routeId/nodeId/nodeType/cardPolicy/completionPolicy`。
- 探索会话已提供 `mapId/routeId/currentNodeId/completedNodeIds/learningSessionIds/unclaimedReceiptIds`、剩余卡片和 `returnContext`。
- 单词活动结果只携带地图上下文，不写入 Profile、FSRS 或掌握度。
- 森林 P1 垂直切片的实现与契约已 verified；浏览器已实际走查入口、NPC、学习屋、溪流、单词探险屋、农场、检查点和传送门节点选择，活动节点可生成带上下文的单词地图 URL。
- 浏览器已确认活动 URL 保留原 `mapId/routeId/nodeId/taskId`、学习会话、未领奖励、剩余卡片和返回地址；用 fixture 返回 URL 加载地图后，原活动节点、任务和已完成节点可恢复，刷新后保持。
- 历史首轮真实活动只推进到 `6/50`，未到达活动完成页；后续回合已真实完成 `50/50` 并返回原节点。旧的 `360x800` 横溢出证据已由 MAP-P1-02 的新鲜几何复测覆盖。

## 阻塞

旧的浏览器验收阻塞记录：首轮活动交互受动态目标影响只到 `6/50`，并曾存在移动端横向溢出；这些历史证据保留用于追溯。后续真实活动完成/返回已通过，MAP-P1-02 也已修复并复测桌面与 `360x800` 横溢出。当前未修改 CORE bridge；若后续需要宿主自动导航，提交接口需求给 CORE，不直接改共享协议。

## 最新回合补充（2026-07-27）

- 历史记录保留：上一轮真实活动只到 `6/50`，旧的动态目标阻塞和 360x800 约 30px 溢出描述不删除；本节记录最新结果，不替换历史证据。
- 真实浏览器第二次森林活动完整推进到 `50/50`，完成页显示“已完成 50 个单词”，完成页返回按钮在新布局下通过真实鼠标点击成功导航回探索地图。该结果不是 fixture/Node 模拟。
- 返回 URL 的 `returnContext` 解码为：`mapId=forest-farm`、`routeId=forest-farm-main-v1`、`nodeId=forest-explorer-03`、`taskId=forest-word-explorer`、`completedNodeIds=[forest-entry, forest-guide-01]`、`learningSessionIds=[browser-session-1, word-memory-ms2q2rx9-0rgyul]`、`unclaimedReceiptIds=[receipt-browser-1]`、`remainingCardIds=[]`、`returnUrl=http://127.0.0.1:7003/games/explore-map/?mapId=forest-farm`。返回后的页面实际选中“单词探险屋”，对应 `currentNodeId=forest-explorer-03`，未完成路线仍保留。
- 完成页布局核对：`innerWidth=1280`、`innerHeight=720`；`.finish-card` `clientHeight=694`、`max-height=696px`、`overflow-y=auto`，返回按钮初始位置 `y=755.59` 但可在卡片内部滚动并被真实点击；修复前的点击失败坐标和错误保留在执行日志中。
- P1-02 修复前横溢出证据：返回后的真实桌面地图 `scrollWidth=1286`、`clientWidth=1265`，唯一越界容器为 `#pixelStoryShell`（宽 `1286`）；历史 `360x800` 实测为 `scrollWidth=375`、`clientWidth=345`。当时浏览器控制能力固定为 `1280x720`，不能据此冒充移动端复测。
- 修复前最终状态：MAP-P1-01 实现/契约 verified；真实活动完成与完成页返回 verified；P1-02 的横向溢出仍待修复，整体保持 `browser-partial-blocked/review`。未修改 CORE、bridge/host、Profile、FSRS、学习算法或奖励结算。

## MAP-P1-02 历史范围冲突（已解除）

- 实际溢出页面是 `games/explore-map/index.html`，其 `#pixelStoryShell` 样式来自 `vendor/css/pixel-story.css`；该页面不加载 `games/word-memory-map/styles.css`。
- `vendor/css/pixel-story.css` 的 `#page-explore .pixel-story-shell` 设置 `width: min(1240px, 100%)`、`padding: 22px`、`overflow: visible`，且当前没有可确认的全局 `box-sizing: border-box`，与历史 `scrollWidth=1286/clientWidth=1265` 相符。
- 历史原授权只允许 `games/word-memory-map/styles.css` 及同目录页面样式；该范围无法改变真实 explore-map 页面，因此当时曾标记为 `scope-blocked`。
- 主控随后批准的最小范围为 `games/explore-map/index.html` 局部覆盖和地图专项测试，明确禁止修改全局 `vendor/css/pixel-story.css`。
- 该范围冲突已解除，当前 MAP-P1-02 已完成最小实现并进入 `review`，不再以 `scope-blocked` 作为当前任务状态。

## MAP-P1-02 最新回合（2026-07-27 13:02 +08:00）

- 实现：在 `games/explore-map/index.html` 局部样式中为 `#page-explore`、`#pixelStoryMapHost` 增加 `min-width:0`、`max-width:100%`、`overflow-x:hidden`；为 `.pixel-story-shell` 增加 `box-sizing:border-box`、`max-width:100%`。没有修改全局 `vendor/css/pixel-story.css`。
- 断言：`tests/test-forest-vertical-slice.mjs` 增加页面根容器、故事地图宿主和故事壳的布局约束断言；现有节点、完成页和 `returnContext` 断言保持不变。
- 真实桌面证据：视口 `innerWidth=1280`、`innerHeight=720`；document/body `scrollWidth=clientWidth=1265`；`#page-explore` 和 `#pixelStoryMapHost` 均为 `1265px`，`#pixelStoryShell` 为 `1240px`、`box-sizing:border-box`，无横向溢出。
- 真实移动证据：通过浏览器视口能力实际设置 `360x800`；document/body `scrollWidth=clientWidth=345`；`#page-explore` 和 `#pixelStoryMapHost` 均为 `345px`，`#pixelStoryShell` 为 `345px`、`box-sizing:border-box`，无横向溢出。该证据不是历史桌面数据推断。
- 命令证据：`npm run test:map`、`npm test`、`node --check games/explore-map/exploration-session.js`、`node --check games/explore-map/forest-route.js`、`node --check games/word-memory-map/game.js`、目标范围 `git diff --check` 全部退出 0；回归包含 3 张地图、森林垂直切片、探索会话、11 个小游戏、6324 张卡片和完整模式。
- 当前结论：MAP-P1-02 为 `review`，桌面与 `360x800` 几何验收通过；等待主控核对全局矩阵是否需要同步，不扩大到 CORE、bridge/host、Profile、FSRS、学习算法、奖励结算或其他游戏。
