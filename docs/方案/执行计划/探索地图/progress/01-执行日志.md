# 探索地图执行日志

## 日志格式

```text
时间：
任务：
动作：
命令：
结果：
验收项：
风险/下一步：
```

## 日志

时间：2026-07-27
任务：MAP-P0-01、MAP-P0-02
动作：补齐森林、太空、外星三张探索地图的统一路线节点契约；新增探索会话存储与 `returnContext` 编解码；单词地图活动结果携带原地图、路线、节点、任务、学习会话、剩余卡片和未领奖励引用。
命令：`npm test`；`npm run test:map`；`node --check games/explore-map/exploration-session.js`；`node --check games/word-memory-map/game.js`；`git diff --check`
结果：地图契约测试通过（3 maps）；探索会话契约测试通过；小游戏总契约通过（11 games）；完整内容契约通过（6324 cards）；完整模式运行时契约通过；JavaScript 语法检查通过。
验收项：MAP-A01、MAP-A02、MAP-A03、MAP-A05、MAP-A06、MAP-A08 的数据/契约部分通过；未修改学习算法、宿主桥接、Profile、FSRS 或奖励结算。
共享文件说明：`package.json` 为纳入地图专项测试新增 `test:map` 脚本并接入 `test`，现有版本保持 `0.1.1`；除此之外不扩大共享文件修改范围。
风险/下一步：MAP-P1-01 仍需浏览器验证森林 RPG 垂直切片闭环；宿主自动导航若需要共享 bridge 变更，另由 CORE 线程评估接口。

时间：2026-07-27
任务：MAP-P1-01
动作：复用 `data/maps-manifest.json` 与探索会话契约，新增森林路线控制器和响应式路线 UI；支持选择节点、记录节点完成、从单词探险屋发起学习活动，以及在活动完成页携带 `returnContext` 返回原节点。
命令：`npm run test:map`；`npm test`；`node --check games/explore-map/exploration-session.js`；`node --check games/explore-map/forest-route.js`；`node --check games/word-memory-map/game.js`
结果：地图契约、森林垂直切片、探索会话、小游戏总契约、完整内容契约和完整模式运行时契约全部通过；完整内容仍为 6324 cards。
验收项：自动化验证覆盖入口状态、NPC/活动节点、节点选择、活动 URL、原节点恢复、未完成路线保留和页面接线；未修改 bridge/host/Profile/FSRS/学习算法。
浏览器验收：未执行真实浏览器探索，MAP-A04/MAP-A05 的浏览器部分、移动端布局和实际活动返回暂记为未验收。

最终状态：MAP-P1-01 实现与契约 verified；浏览器 review 未执行，MAP-A04/MAP-A05 浏览器部分和移动端布局保持未验收。未扩大本次文件范围。

时间：2026-07-27
任务：MAP-P0-01、MAP-P0-02 复核
动作：复核三张主地图的自由进入、统一路线节点字段和配置驱动活动声明；复核探索会话的当前任务、学习会话、剩余卡片、未领奖励和 `returnContext` 恢复字段。实现已满足契约，本次未重复改动地图或小游戏代码。
命令：`npm run test:map`；`npm run test:complete`；`node --check games/explore-map/exploration-session.js`；`node --check games/explore-map/forest-route.js`；`node --check games/word-memory-map/game.js`
结果：地图契约通过（3 maps）；森林垂直切片契约通过；探索会话契约通过；完整内容契约通过（6324 cards）；完整模式运行时契约通过；JavaScript 语法检查通过。
验收项：MAP-A01、MAP-A02、MAP-A03、MAP-A05、MAP-A06、MAP-A08 的自动化/数据部分复核通过；三张地图均为 `freeEntry: true` 和 `recommended-not-locked`；未修改小游戏学习算法、host/bridge.js、Profile、FSRS 或奖励结算。
风险/下一步：真实浏览器走查和移动端布局仍属于既有未验收项；本次只完成 P0 契约与会话的自动化复核。

时间：2026-07-27
任务：MAP-P1-01 浏览器验收
动作：启动本地服务 `http://127.0.0.1:7003`，实际走查森林入口、NPC、学习屋、单词探险屋节点选择和活动启动；解析活动 URL 中的 `returnContext`，验证原地图、路线、节点、任务、学习会话、未领奖励、剩余卡片和返回地址均被携带。活动实际进入单词地图并完成 6 张卡的浏览器交互，但未到达完整活动完成页。
浏览器证据：桌面页面入口默认选中 `forest-entry`；NPC、学习屋、单词探险屋按钮均可唯一定位并切换详情；活动 URL 恢复为 `mapId=forest-farm`、`routeId=forest-farm-main-v1`、`nodeId=forest-explorer-03`、`taskId=forest-word-explorer`。用从实际上下文派生的 fixture 返回 URL 加载地图后，页面显示 2 个已完成节点、单词探险屋仍选中，刷新后状态保持。
移动端证据：`360x800` 下 8 个森林节点卡片均在视口内且无节点卡片相互重叠；NPC 和单词探险屋可点击，活动按钮在视口内可见并能跳转到带 `returnContext` 的活动 URL。布局测量发现 `document.scrollWidth=375`、`clientWidth=345`，存在约 30px 横向溢出，顶部/下方内容有裁切，因此 MAP-A09 不通过。
浏览器阻塞：当前 in-app browser 中活动敌人持续移动，部分目标在坐标读取和点击之间移出视口；活动仅推进到 6/50，未打开完成页，故真实“完成 -> 返回原节点”不宣称通过。此前的自动化/契约证据仍有效，但不能替代 MAP-A04/MAP-A05 浏览器完成返回验收。
范围：未修改 `bridge.js`、host、Profile、FSRS、学习算法或奖励结算；本次仅更新探索地图 task/progress 文档。

时间：2026-07-27
任务：MAP-P1-01 森林垂直切片浏览器收口
动作：在真实应用内浏览器中重新执行森林入口 -> 节点选择 -> 活动启动 -> 50 词学习 -> 完成页返回；保留历史 6/50 记录，不以 fixture/Node 事件替代真实活动。
真实浏览器证据：第二次真实活动从 `0/50` 推进到 `50/50`，完成页显示“已完成 50 个单词”；完成页 `#returnToExploreButton` 在修复后的卡片布局中由真实鼠标点击成功导航到 `http://127.0.0.1:7003/games/explore-map/?mapId=forest-farm&returnContext=...`。
完成页布局证据：修复后 `innerWidth=1280`、`innerHeight=720`；`.finish-card` 为 `clientHeight=694`、`max-height=696px`、`overflow-y=auto`，返回按钮 `y=755.59`、`bottom=803.59`，点击器自动滚动卡片后点击成功。修复前曾记录按钮 `y=703.59` 且点击报错 `Unable to translate Input.dispatchMouseEvent ... No element found at point 640,727.59375`，该失败证据保留以说明修复原因。
返回上下文证据：解码后的 `returnContext` 为 `mapId=forest-farm`、`routeId=forest-farm-main-v1`、`nodeId=forest-explorer-03`、`taskId=forest-word-explorer`、`completedNodeIds=[forest-entry, forest-guide-01]`、`learningSessionIds=[browser-session-1, word-memory-ms2q2rx9-0rgyul]`、`unclaimedReceiptIds=[receipt-browser-1]`、`remainingCardIds=[]`、`returnUrl=http://127.0.0.1:7003/games/explore-map/?mapId=forest-farm`。返回地图实测选中“单词探险屋”，对应 `currentNodeId=forest-explorer-03`；未完成路线未被清空。
横溢出与移动检查：返回地图真实桌面测得 `scrollWidth=1286`、`clientWidth=1265`、`innerWidth=1280`，唯一越界容器为 `#pixelStoryShell` 宽 `1286`。历史 360x800 记录为 `scrollWidth=375`、`clientWidth=345`，约 30px 溢出且内容裁切；本回合 `browser.tabs.new({width:360,height:800})` 和 `{viewport:{width:360,height:800}}` 都仍返回 `1280x720`，`window.resizeTo` 抛出 `TypeError: window.resizeTo is not a function`，故未虚构新的 360x800 结果。
fixture/Node 与真实浏览器区分：`npm run test:map`、`npm test`、契约和语法检查只证明配置、探索会话、returnContext 编解码及页面接线；它们不替代本日志中的 50/50 和完成页真实点击。旧日志中的 fixture 返回恢复证据同样不替代本次真实返回。
命令/范围：本回合继续使用 `http://127.0.0.1:7003/` 本地服务；完成页短视口修复仅在 `games/word-memory-map/styles.css`，地图专项测试增加对应 CSS 约束断言；未修改 `package.json`（既有日志已记录其 `test:map` 变更和版本 `0.1.1`），未修改 `bridge.js`、host、Profile、FSRS、学习算法、奖励结算或其他游戏目录。
结果：MAP-P1-01 实现/契约 verified；真实活动完成和完成页返回 verified；360x800 复测受当前浏览器固定视口能力阻塞，桌面地图横向溢出仍未通过，整体状态保持 `browser-partial-blocked/review`。下一步仅为 MAP 范围内的 360x800 实机复测和 `#pixelStoryShell` 横溢出最小修复评估。

时间：2026-07-27 12:56:16 +08:00
任务：MAP-P1-02 横溢出和移动布局收口
动作：核对实际页面加载链路、样式选择器和既有浏览器几何证据；未修改代码。确认 `games/explore-map/index.html` 只加载 `vendor/css/pixel-story.css`、`games/explore-map/forest-route.css`，而 `games/word-memory-map/styles.css` 不参与 `#pixelStoryShell` 布局。
证据：`vendor/css/pixel-story.css` 的 `#page-explore .pixel-story-shell` 使用 `width:min(1240px,100%)`、`padding:22px`、`overflow:visible`；在未统一 `box-sizing:border-box` 时外宽可达到约 1286px，与历史真实浏览器 `scrollWidth=1286/clientWidth=1265` 一致。历史 `360x800` 的 `375/345` 横溢出仍保留；当前浏览器无法调整到该视口，未新增或虚构移动端证据。
命令：`npm run test:map`（PASS map contract: 3 maps；PASS forest vertical slice contract；PASS exploration session contract）。
结果：MAP-P1-02 标记 `scope-blocked`，因为当前授权写入范围无法触达真实溢出来源。已向主控报告，建议批准 `games/explore-map/index.html` 局部样式或 `vendor/css/pixel-story.css` 的最小覆盖；在批准前不改 vendor/explore-map，不把基线契约通过当作布局修复。

时间：2026-07-27 13:02:52 +08:00
任务：MAP-P1-02 横溢出和移动布局收口
动作：按主控批准范围，在 `games/explore-map/index.html` 增加局部布局约束，并在地图专项测试中加入页面几何样式断言；保留既有节点、完成页和 `returnContext` 行为。
命令：`npm run test:map`；`npm test`；`node --check games/explore-map/exploration-session.js`；`node --check games/explore-map/forest-route.js`；`node --check games/word-memory-map/game.js`；`git diff --check -- games/explore-map/index.html tests/test-forest-vertical-slice.mjs docs/方案/执行计划/探索地图`
结果：全部退出 0。地图专项通过 3 张地图、森林垂直切片和探索会话；全局回归通过 11 个小游戏、6324 张卡片和完整模式；语法检查通过；diff 检查无空白错误，仅保留既有 LF/CRLF 警告。
浏览器证据：真实桌面 `1280x720` 测得 document/body `1265/1265`，`#pixelStoryShell` `1240px` 且 `box-sizing:border-box`；真实 `360x800` 测得 document/body `345/345`，`#pixelStoryShell` `345px` 且 `box-sizing:border-box`。两种视口均无横向溢出。
验收项：MAP-P1-02 的局部 CSS 约束、桌面横溢出和 `360x800` 几何通过；节点、完成页、`returnContext` 的现有专项断言继续通过。当前任务改为 `review`，不再是 `scope-blocked`。
范围/风险：只修改 `games/explore-map/index.html`、`tests/test-forest-vertical-slice.mjs` 和探索地图自身三份记录；未修改全局 `vendor/css/pixel-story.css`、CORE/bridge/host、Profile、FSRS、学习算法、奖励结算或其他游戏。下一步由主控复核全局验收矩阵，不以本轮几何证据升级其他线程验收。
