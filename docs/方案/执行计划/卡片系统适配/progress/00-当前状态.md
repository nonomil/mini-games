# 卡片系统适配当前状态

```text
status: capability-blocked
current_task: ADAPTER-P2-02
owner_thread: ADAPTER
project: G:\StudyCode\卡片式单词学习游戏记忆系统
  last_verified_at: 2026-07-29
```

## 当前判断

- 卡片系统已经具备 `gamePolicy`、session、progress、rewards 和 full-game adapter 基础。
- 第一接入目标是苦力怕和飞机大战；拼音赛车和网球分别按 domain/contentType 接入。
- ADAPTER-P0-01 已用 fixture 验证 `ready -> init -> card-result -> complete/result -> onComplete -> stop`；三张卡的顺序和 `cardId` 保持不变。
- ADAPTER-P1-01 已完成三张 CORE manifest/URL 到稳定 adapter 配置的映射，init 透传冻结协议元数据，并保持 legacy `source/kind/result` 兼容。
- ADAPTER-P1-02 已完成章节卡片 payload 与 card-result 归一化：保留 session/context/card/domain/contentType，补齐学习证据字段，并保持 complete/result 只走 adapter 回调边界。
- ADAPTER-P1-03 已完成 adapter/host fixture 的 session 恢复边界：宿主按 `sessionId + gameId` 恢复原卡片集合和 `returnContext`，按 `cardId` 幂等拒绝重复结果；完成 summary 透传顶层 session/game/context，stop 后直接提交和消息提交均被拒绝。
- ADAPTER-P2-01 已完成成长、AI、每日计划接口边界 fixture：学习事件字段完整映射到 host，XP/等级/连胜/勋章交给 growth delegate，既有 rewards 只接收 points/experience；AI 失败回退到确定性解释/例句/原计划；balanced 计划覆盖到期词、错词和少量新词。
- ADAPTER-P2-02 的证据必须分层：fixture/adapter contract 只证明卡片系统边界；HTTP/静态入口只证明页面可达；真实浏览器 postMessage 和真实宿主接线目前没有闭环证据。
- 替换矩阵：`typing-defense` 使用 `games/typing-defense/web/index.html`、逻辑 `gameId=typing-defense`、domain `english`、adapter contentType `english-word-typing`、legacy `petbank-typing-defense/result`；`word-shooter` 与 `pinyin-racer` 共用 `games/learning-arcade/index.html`，分别使用 query `game=word-shooter`/`game=word-cannon`、domain/contentType `english/english-word-typing` 与 `pinyin/pinyin-choice`，均使用 legacy `petbank-learning-arcade/result`。
- `tennis-english` 在小游戏项目有独立 `game-manifest.json`（domain `english-expression`、contentType `expression`），但卡片系统 `FULL_GAME_ADAPTER_CONFIGS` 尚无 mapping；不能将它纳入真实替换闭环。
- 外部只读服务返回了四个入口的 HTTP 200；同源真实浏览器 harness 已完成 typing-defense 的 parent/iframe 检查：parent 发送 `type:init` 后页面进入“宿主卡片”模式，但游戏没有发送 `ready` 或 `card-result`，只回传 legacy `petbank-typing-defense/result`；完整 CORE 生命周期、恢复/重复/stop 后拒绝仍未取得真实证据。

## P0 兼容结论

- 当前 bridge 别名为 `word-quest-host` / `word-quest-game-bridge`，真实小游戏结果 source 为四个 legacy source；adapter 已增加 `kind: complete` 到现有 summary 路径的兼容映射。
- 当前 init 传递 `cards/autostart/sourceGame/mode` 及冻结的 `protocolVersion/sessionId/gameId/domain/contentType/seed/returnContext`；legacy `source/kind` 与现代 `type` 并存。
- 当前结果归一化保留 `cardId/correct/grade/responseMode/responseMs/hintUsed/independent/sourceGame/mode`，并补齐 `sessionId/returnContext/domain/contentType/errorTags/operationErrors`。
- `complete/result` 只驱动本局 summary 和完成回调，不直接写 Profile、章节、FSRS 或 reward receipt；实际结算仍由宿主回调负责。
- 缺少可选卡片字段会降级为 `null`；缺少 `cardId` 或 domain/contentType 不匹配拒绝初始化；配置 `allowedOrigin` 后非法来源不会触发 init/result。
- 最新验证：focused adapter/growth tests 28/28、`npm test` 142/142、`npm run test:contract` 13/13，临时 browser harness 语法检查通过，均为 0 fail；这些内部测试与单入口浏览器证据仍不足以提升真实跨项目接线等级。

## 阻塞

CORE-P0-03 已 verified，但 ADAPTER-P2-02 当前为 capability-blocked：真实入口仍使用 legacy `source/kind`，typing-defense、word-shooter、pinyin-racer 尚未真实接入 CORE v1，tennis-english 尚无卡片 adapter mapping，且没有真实 postMessage/宿主闭环。fixture、HTTP 200 或静态 manifest 均不能替代真实接线证据；后续需要获得可用浏览器授权并由真实宿主完成验证，仍须保持游戏源代码只读。

## 本线程复核

- 2026-07-27 重新核对 adapter、宿主消息边界、gamePolicy、session、progress、rewards 及 fixture；P0-01 兼容字段表和第一版映射建议与实际实现一致。
- 新鲜验证：adapter focused suite `21/21`、`npm test` `127/127`、`npm run test:contract` `12/12`，均 `0 fail`。
- 复核没有扩大接入范围；真实外部小游戏仍未接线，后续必须等待对应协议和接入任务。

## 2026-07-29 复核

- 使用 `tests/fixtures/real-browser-host-harness.mjs` 通过同源 parent/iframe proxy 检查真实 typing-defense 页面：`type:init` 可被页面接受并显示宿主卡片；消息记录为 parent -> game `init`、game -> parent legacy `result`，没有 `ready` 或 `card-result`。
- 新鲜验证：adapter/growth focused `28/28`、`npm test` `142/142`、`npm run test:contract` `13/13`，harness `node --check` 通过。
- P2-02 继续保持 `capability-blocked`：真实小游戏尚未输出完整 CORE v1 生命周期，未修改小游戏源代码、CORE bridge、Profile、FSRS、progress 或 rewards。
