# 卡片系统适配执行日志

## 日志格式

```text
时间：
任务：
动作：
命令：
结果：
验收项：
风险/下一步：
```

## 日志

时间：2026-07-27
任务：ADAPTER-P0-01
动作：阅读主控/CORE/适配计划、验收矩阵、宿主 bridge、full-game adapter、gamePolicy、session/progress/rewards 及相关测试；新增 iframe/window fixture；增加 `complete` 到 legacy summary 的兼容别名。
命令：`node --test tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`
结果：通过，16 tests / 16 pass / 0 fail。
验收项：ADAPTER-A01、ADAPTER-A02 的 fixture 证据；ready/init/card-result/complete/result/stop 路径、三张卡顺序、重复消息、停止清理均已断言。
风险/下一步：当前 init 未传 `protocolVersion/sessionId/gameId/domain/contentType/seed/returnContext`，结果未透传 `errorTags/operationErrors/sessionId`，`targetOrigin` 仍为 `*`；待 CORE 协议 verified 后进入 ADAPTER-P1-01/P1-02。

时间：2026-07-27
任务：ADAPTER-P0-01
动作：执行完整项目回归与页面结构契约回归。
命令：`npm test`；`npm run test:contract`
结果：`npm test` 122 tests / 122 pass / 0 fail；`npm run test:contract` 12 tests / 12 pass / 0 fail。
验收项：ADAPTER-A02、ADAPTER-A09；新增 adapter fixture 未破坏现有 session/progress/rewards、项目契约或发布边界。
风险/下一步：CORE 仍为 draft/planned，真实小游戏协议校验、origin 收紧、session/domain/contentType/errorTags 完整传递留给后续任务。

时间：2026-07-27
任务：ADAPTER-P0-01
动作：最终复核发现并修复 `full-game-adapter` 未移除 iframe `load` listener 的清理缺口；fixture 增加 listener 清理断言后重新执行 focused、全量和 contract 回归。
命令：focused adapter tests；`npm test`；`npm run test:contract`
结果：focused 16/16；`npm test` 122/122；`npm run test:contract` 12/12；均 0 fail。
验收项：ADAPTER-A01、ADAPTER-A02、ADAPTER-A09；卡片顺序、生命周期、重复消息、complete/result、stop 清理及发布边界均有新鲜证据。
风险/下一步：没有声称真实小游戏已接入；待 CORE 将协议标记为 verified 后，继续 ADAPTER-P1-01/P1-02。

时间：2026-07-27
任务：ADAPTER-P0-01
动作：补充 legacy `kind: result` 正向完成 fixture，确保 `result` 与 `complete` 两条结束路径都有独立成功断言。
命令：focused adapter tests；`npm test`；`npm run test:contract`
结果：focused 17/17；`npm test` 123/123；`npm run test:contract` 12/12；均 0 fail。
验收项：ADAPTER-A02 的 ready/init/result/complete/stop 全路径正向覆盖。
风险/下一步：CORE 仍为 draft/planned；没有声称真实小游戏已接入。

时间：2026-07-27
任务：ADAPTER-P1-01
动作：读取 CORE manifest/schema 与已冻结协议字段，先补充 manifest/URL、三卡顺序、cardId、metadata、缺字段和 origin 的失败 fixture，再实现 adapter 稳定配置与 modern/legacy 消息兼容。
命令：adapter fixture red phase；`node --test tests/test-full-game-adapter-fixture.mjs`
结果：5 tests / 1 pass / 4 fail；失败点对应尚未实现的 manifest exports、metadata/cardId/null mapping 和 origin validation，作为实现前失败证据保留在执行记录中。
验收项：TDD 先红后绿；未修改小游戏源代码，也未写入 Profile、FSRS、progress 或 reward。
风险/下一步：真实小游戏尚未接线；后续继续处理 P1-02 的章节 payload 与结果归一化边界。

时间：2026-07-27
任务：ADAPTER-P1-01
动作：完成 adapter 侧三张 CORE manifest/URL 映射、init 冻结元数据透传、卡片顺序与 cardId 保持、可选字段降级、非法 origin 拒绝、complete/result/stop 生命周期和 listener 清理。
命令：`node --test tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：focused 20/20；全量 126/126；contract 12/12；均 0 fail。
验收项：ADAPTER-P1-01；三份 manifest、相对 URL、protocolVersion/sessionId/gameId/domain/contentType/seed/returnContext、legacy source/kind/result、complete/stop、缺字段和非法 origin 均有 fixture 或契约证据。
风险/下一步：CORE-P0-03 已 verified，但真实小游戏尚未接线；本次只完成 adapter mapping，不改变 Profile/FSRS/progress/reward 规则。

时间：2026-07-27
任务：ADAPTER-P1-02
动作：先补充章节 payload 与结果归一化失败 fixture，断言卡片顺序/cardId、默认与显式 domain/contentType、sessionId/returnContext、errorTags/operationErrors/responseMode/responseMs/hintUsed/independent，以及 complete 汇总上下文。
命令：`node --test tests/test-full-game-adapter-fixture.mjs`
结果：6 tests / 5 pass / 1 fail；失败原因是实现尚未透传章节卡片 `domain`，证明红 fixture 捕捉到功能缺口。
验收项：TDD 红阶段；未修改小游戏源代码，也未写入 Profile、FSRS、progress 或 reward。
风险/下一步：实现 card payload/result mapping 后重新执行 focused、全量和 contract 回归。

时间：2026-07-27
任务：ADAPTER-P1-02
动作：实现章节卡片字段透传与默认值、full-game card-result 统一归一化、legacy source/kind/result 兼容和 complete summary 上下文回填；保持 Profile/FSRS/reward 只由宿主回调负责。
命令：`node --test tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：focused 21/21；全量 127/127；contract 12/12；均 0 fail。
验收项：ADAPTER-P1-02；章节 payload、sessionId/returnContext/cardId/domain/contentType、errorTags/operationErrors/responseMode/responseMs/hintUsed/independent、complete/result 和 legacy 兼容均有新鲜证据。
风险/下一步：CORE-P0-03 已 verified，但真实小游戏尚未接线；本次只完成 adapter payload/result mapping，不改变 Profile/FSRS/progress/reward 规则。

时间：2026-07-27 07:55:04 +08:00
任务：ADAPTER-P0-01
动作：本线程复核 `full-game-adapter`、宿主消息协议、gamePolicy、session/progress/rewards 边界和 adapter fixture；确认兼容字段表、第一版映射建议及 P0 生命周期证据仍与实际代码一致。
命令：`node --test tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：adapter focused suite `21/21`；全量 `127/127`；contract `12/12`；均 `0 fail`。
验收项：ADAPTER-A01、ADAPTER-A02、ADAPTER-A09；fixture 覆盖 `ready/init/card-result/result/complete/stop`，并验证卡片顺序、重复结果、停止清理和失败可观察性。
风险/下一步：本线程未修改小游戏源代码、Profile、FSRS、progress 或 reward 规则；真实外部小游戏接入仍未完成，继续留给 CORE verified 后的后续适配任务。

时间：2026-07-27
任务：ADAPTER-P1-03
动作：先新增 `HostSessionFixture` 和恢复/幂等/stop/返回上下文失败测试，验证同 `sessionId + gameId + cardId` 的宿主状态可恢复；红阶段为 8 tests / 7 pass / 1 fail，失败点为完成 summary 缺少顶层 session 上下文。随后让 full-game adapter 识别宿主 duplicate 响应，并在完成 summary 透传 `sessionId/gameId/returnContext`。
命令：`node --test tests/test-full-game-adapter-fixture.mjs`；`node --test tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：focused adapter suite `23/23`、全量 `npm test` `129/129`、contract `12/12`，均 `0 fail`。首次全量尝试曾因静态发布测试复制产物时 `ENOSPC` 失败；清理两个已忽略的生成目录 `_site_verify`/`_site_game_verify` 后重跑通过。
验收项：ADAPTER-A02、ADAPTER-A04、ADAPTER-A05、ADAPTER-A06、ADAPTER-A09；fixture 覆盖刷新后相同 session/card、returnContext init/complete 透传、宿主 duplicate 结果、stop 后 adapter.submit 和 message listener 拒绝。
风险/下一步：本轮只修改卡片系统 `js/games`、adapter fixture/测试及本工作流 task/progress；未修改小游戏源代码、Profile、FSRS、progress 或 reward 规则。fixture 通过不代表真实跨项目接线完成，真实外部小游戏仍待后续接入与运行验证。

时间：2026-07-27
任务：ADAPTER-P2-01
动作：先新增 `tests/test-full-game-growth-boundary.mjs`，覆盖 adapter 学习证据到 host event/receipt、growth delegate、AI 成功/失败 fallback、balanced 到期/错词/少量新词计划，以及同 session/card 恢复、重复 receipt、stop 后拒绝；红阶段因缺少 `full-game-growth-boundary-fixture` 模块失败。随后新增测试专用 `createGrowthBoundaryHost` 和 `createCoachBoundary` fixture，未修改生产 adapter、router、session、progress 或 rewards 规则。
命令：`node --test tests/test-full-game-growth-boundary.mjs`；`node --test tests/test-full-game-growth-boundary.mjs tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：growth boundary `5/5`；focused adapter/growth suite `28/28`；全量 `npm test` `134/134`；contract `12/12`，均 `0 fail`。
验收项：输入事件到 receipt 的字段映射、XP/等级/连胜/勋章委托、AI 成功与确定性回退、到期/错词/少量新词计划、重复/恢复/stop 均有 fixture contract 证据；实际 rewards receipt 仍只使用既有 `points/experience` 字段。
风险/下一步：本轮只新增 adapter/host/growth/coach fixture 和专属测试，并更新本工作流 task/progress；未修改小游戏项目源代码、CORE bridge、Profile、FSRS、progress 或 reward 规则。真实跨项目游戏、真实成长服务和 AI provider 仍 pending，fixture 通过不代表真实接线完成。

时间：2026-07-27
任务：ADAPTER-P2-01
动作：在 P2 fixture 完成后重新执行 focused、全量、contract、语法和范围审计；确认本轮只保留 growth/coach boundary fixture 与专属测试，并未生成或修改词库 runtime catalog。
命令：`node --test tests/test-full-game-growth-boundary.mjs tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`；`node --check tests/fixtures/full-game-growth-boundary-fixture.mjs`；`node --check tests/test-full-game-growth-boundary.mjs`；`git diff --check`
结果：focused 28/28；全量 `npm test` 138/138；contract 12/12；两份 P2 文件语法检查和 `git diff --check` 均通过，均为 0 fail。
验收项：输入事件到 receipt 字段映射、XP/等级/连胜/勋章委托、AI 成功与确定性回退、到期/错词/少量新词计划、重复/恢复/stop 边界均保持新鲜 fixture contract 证据；既有 rewards receipt 仍只使用 `points/experience`。
风险/下一步：当前工作树含主控/其他任务的既有变更，本轮未将其归入 P2；真实跨项目游戏、真实成长服务和 AI provider 仍 pending，fixture 通过不能代表真实接线完成。ADAPTER-P2-02 跨项目回归与替换验证仍 planned。

时间：2026-07-27
任务：ADAPTER-P2-02
动作：读取卡片系统适配计划/验收矩阵、现有 adapter/session/returnContext/growth fixture 与测试，以及小游戏项目 CORE v1 协议、三份 manifest、tennis manifest 和真实入口源码；对外部项目只执行只读 HTTP/源码检查。尝试真实浏览器入口后，因 parent/iframe 检查持续处于授权等待，按主控指令终止浏览器尝试并收口为 capability-blocked；本轮没有新增生产 adapter、游戏代码或跨项目测试代码。
证据分层：
- fixture/adapter contract：既有 `tests/test-full-game-adapter-fixture.mjs`、`tests/test-full-game-growth-boundary.mjs` 验证 adapter 配置、session/card/returnContext、重复和 stop 边界；这是卡片系统内部证据。
- HTTP/静态入口：小游戏项目本地只读服务返回 `200`：`games/typing-defense/web/index.html?host=1&autostart=1&mode=words`、`games/learning-arcade/index.html?host=1&game=word-shooter`、`games/learning-arcade/index.html?host=1&game=word-cannon`、`games/tennis-english/index.html`；这只证明入口可达。
- 真实页面/源码：typing-defense manifest 为 `gameId=typing-defense`、domain `english` 等多 domain，入口仍发送 `source=petbank-typing-defense`/`kind=result`；word-shooter 与 pinyin-racer 共用 learning-arcade 入口，分别通过 `game=word-shooter`/`game=word-cannon`，均发送 `source=petbank-learning-arcade`/`kind=result`；源码未证明三者已真实使用 CORE v1 envelope。
- 真实 postMessage/宿主接线：未取得 `ready -> init -> card-result -> complete -> stop`、sessionId/cardId/returnContext、重复结果或 stop 后拒绝的真实 parent/iframe 闭环证据；浏览器授权等待已中止。
- 网球边界：`games/tennis-english/game-manifest.json` 存在，domain 为 `english-expression`、contentType 为 `expression`，但卡片系统 `FULL_GAME_ADAPTER_CONFIGS` 没有 `tennis-english` mapping，不能宣称纳入 P2-02 替换接线。
命令：外部服务只读 `Invoke-WebRequest` 四个入口；卡片系统 focused adapter/growth suite；`npm test`；`npm run test:contract`。
结果：focused `28/28`；卡片系统全量 `139/139`；contract `13/13`，均 `0 fail`。HTTP 200 和内部测试均不改变真实接线状态；浏览器 postMessage/宿主层为 capability-blocked。
验收项：替换矩阵已覆盖 typing-defense、word-shooter、pinyin-racer 的 entry/gameId/domain/contentType/legacy alias；tennis mapping 缺口已记录；fixture、HTTP、真实页面、真实 postMessage/宿主四层证据已明确区分。
风险/下一步：ADAPTER-P2-02 保持 `capability-blocked/review`，不得标记 `verified`。下一步需要可用浏览器授权和真实宿主页面，重新验证三游戏的 v1 生命周期、恢复、重复和 stop；在此之前不修改小游戏项目、CORE/bridge、Profile、FSRS、progress/reward 既有语义。

时间：2026-07-29 00:38:12 +08:00
任务：ADAPTER-P2-02
动作：新增卡片系统范围内的临时 `real-browser-host-harness` fixture，通过同源 parent/iframe proxy 加载真实 typing-defense 页面；发送完整 `type:init`，检查宿主卡片状态和真实回传消息。随后关闭临时服务与浏览器页，执行 adapter/growth focused、全量和 contract 回归。
命令：`node --check tests/fixtures/real-browser-host-harness.mjs`；真实浏览器 harness `http://127.0.0.1:7004/__adapter-harness.html`；`node --test tests/test-full-game-growth-boundary.mjs tests/test-full-game-adapter-fixture.mjs tests/test-game-adapter-contract.mjs tests/test-word-racer-adapter.mjs tests/test-pixel-word-adventure-adapter.mjs tests/test-creeper-typing-adapter.mjs tests/test-air-battle-typing-adapter.mjs`；`npm test`；`npm run test:contract`
结果：真实页面接受 `type:init` 并进入“宿主卡片”模式；消息记录只有 parent -> game `init` 和 game -> parent legacy `source=petbank-typing-defense/kind=result`，没有 `ready` 或 `card-result`。harness 语法通过；focused `28/28`；全量 `142/142`；contract `13/13`，均 `0 fail`。
验收项：ADAPTER-A02、ADAPTER-A06、ADAPTER-A07、ADAPTER-A09；新增真实页面证据确认入口可接收 init，但不能证明完整 CORE v1 生命周期、卡片级结果、恢复、重复或 stop 后拒绝。
风险/下一步：P2-02 保持 `capability-blocked`，不得声称真实接线 verified；typing-defense、word-shooter、pinyin-racer 仍需真实 CORE bridge/宿主接线，tennis-english 仍无卡片 adapter mapping。本轮未修改小游戏源代码、CORE bridge、Profile、FSRS、progress 或 rewards。
