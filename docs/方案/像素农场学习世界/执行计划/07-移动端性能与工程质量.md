# 07 移动端、性能与工程质量

## X-01 大图和资源管线

**Files:**

- Create: `scripts/optimize-image-assets.mjs`
- Create: `scripts/report-asset-budget.mjs`
- Create: `tests/test-asset-budget.mjs`
- Modify: `games/word-memory-map/game-data.js`
- Modify: relevant asset manifests

**Acceptance:** 28 MB 级 PNG 有 WebP/AVIF 候选；移动端使用较小资源；首屏资源报告可重复生成；不删除原始资产，直到视觉回归完成。

## X-02 移动浏览器验收

**Files:**

- Create: `tests/browser/mobile-world.smoke.spec.mjs`
- Modify: `package.json`

**Acceptance:** 360x800、390x844 覆盖进入、移动、点击目标、收获、刷新恢复、返回大厅；桌面 1440x900 覆盖键盘移动和鼠标操作。

## X-03 PWA 和离线壳

**Files:**

- Create: `manifest.webmanifest`
- Create: `service-worker.js`
- Create: `assets/icons/app-192.png`
- Create: `assets/icons/app-512.png`
- Modify: `index.html`
- Create: `tests/test-pwa-contract.mjs`

**Acceptance:** manifest 字段完整；首次在线打开后，应用壳和 starter 词包可以离线打开；缓存版本更新不会无限增长。

## X-04 缓存策略

**Files:**

- Modify: `app.js`
- Modify: `games/word-memory-map/game.js`
- Modify: `local-server.mjs`
- Create: `docs/方案/像素农场学习世界/执行计划/cache-policy.md`

**Acceptance:** 开发环境可及时看到数据变化；生产版本化资源可命中缓存；manifest 更新时有明确版本策略。

## X-05 完整内容回归

**Files:**

- Modify: `tests/contract.mjs`
- Modify: `tests/test-complete-content-contract.mjs`
- Modify: `tests/test-complete-mode-runtime.mjs`

**Acceptance:** 11 个游戏入口、3322 张单词地图词卡、12 个经典场景、80 个故事节点和 261 个宠物仍满足当前契约。

## X-06 可观测性和调试开关

**Files:**

- Create: `vendor/js/debug-log.js`
- Modify: `games/word-memory-map/game.js`
- Modify: `app.js`
- Create: `tests/test-debug-contract.mjs`

**Acceptance:** 可以通过 query 或 local flag 打开加载、存档、学习事件和 bridge 日志；默认用户不会看到调试信息。

## X-07 质量 Gate

**Commands:**

```powershell
npm test
npm run test:contract
npm run test:complete
```

**Acceptance:** 自动化测试全通过；移动端冒烟无阻塞问题；资源预算、PWA、存档和 bridge 无高优先级缺陷。

